# CP-MVP2 Batch 2 — Persisted Lifecycle Integration with CP-06: Execution Report

**Mode: VERIFY → IMPLEMENT → TEST → EXECUTE → EVIDENCE → REPORT → COMMIT.
No Human + Di final gate is declared by this report.**

## A. Governance

* **Starting commit:** `1bc2a9eccf3a03e146cc0ba5620e2821f7e97b7d` (== `origin/main`, confirmed via `git fetch`/`git rev-parse` before any file was created).
* **Ending commit (implementation):** `53a4f385ef5c530e063370a0c63a21b367abcec7`.
* **CR-001 authorization state (preflight, re-verified):** `docs/CP-MVP2-CR-001-PERSISTENT-LIFECYCLE-v1.0.md` and `docs/CP-MVP2-CR-001-ARCHITECTURE-IMPACT-ASSESSMENT-v1.0.md` both read `BATCH 1 IMPLEMENTATION AUTHORIZED AND COMPLETE — AWAITING DI FINAL GATE`; neither document claims Batch 2, CP-07, or any later work is approved or complete. `docs/claude-execution-reports/CP-MVP2-CR-001/CP-MVP2-CR-001-BATCH1-EXEC-20260914-181806-695036.md` and its summary were inspected and are consistent with this state. **No HARD STOP condition was found; implementation proceeded.**
* **Frozen specifications verified (preflight and again before commit):** `git diff --stat 1bc2a9e -- llm/client.py testcases/ testdata/ automation/ execution/{schema,validate,evidence,engine,pipeline}.py requirements/ knowledge/ docs/CP-MVP2-0{3,4,5,6}-SPECIFICATION-v1.0.md` → **empty**, both times.
* **Scope:** integrate Batch 1's persisted artifacts with frozen CP-MVP2-06 execution. One new, additive module (`execution/persisted_lifecycle.py`) plus tests. No redesign of any frozen checkpoint.
* **Exclusions honored:** no requirements/testcase/RBTP/test-data/Playwright generation, no locator invention, no self-healing, no retry beyond what the frozen CP-06 contract already permits, no RCA/replanning, no performance testing, no security/penetration testing, no scope expansion.

## B. Implementation

* **File added:** `execution/persisted_lifecycle.py` — the sole new integration surface.
* **Files modified:** none (frozen or otherwise).
* **Files deliberately untouched:** `execution/{schema,validate,evidence,engine,pipeline}.py` (frozen CP-06), `automation/{schema,generate,validate,pipeline,evidence,persist}.py`, `testdata/{schema,generate,validate,pipeline,persist}.py`, `testcases/{schema,generate,validate,pipeline,persist}.py`, `persistence/envelope.py`, `llm/client.py`, the Approved SRS, every CP-01–06 specification/freeze-checkpoint document.
* **Architecture integration:** `execution/persisted_lifecycle.py` sits strictly between Batch 1's `*/persist.py` modules and the frozen `execution.validate`/`execution.pipeline`. It performs steps 1–10 of the persisted-input contract itself (load, envelope validation, schema deserialization, automation-ID/collision handling, testcase/requirement/test-data/traceability validation) and then hands off to the **unmodified** `execution.pipeline.run_cp_mvp2_06()` for steps 11–12 (eligibility + real execution) — those two steps are never duplicated or reimplemented.

## C. Persistence

* **Artifact locations used (Batch 1's actual, canonical implementation — not reinvented):** `testcases/generated/<testcase_id>/` (`testcases/persist.py`), `testdata/generated/<data_set_id>/` (`testdata/persist.py`), `automation/generated/<automation_id>/` (`automation/persist.py`), all built on `persistence/envelope.py`'s immutable-version + mutable-pointer model.
* **Envelope/version handling:** `resolve_persisted_execution_candidate` calls `persistence.envelope.load_latest`/`load_history`/`load_version` directly — no parallel persistence framework was invented.
* **Loading/validation:** new deserializers (`deserialize_testcase`, `deserialize_dataset`, `deserialize_automation`) reconstruct the exact frozen dataclasses (`GovernedTestcase`, `GovernedTestDataSet`, `GovernedPlaywrightArtifact`) from a persisted payload dict, field-for-field, with no invented defaults beyond what each frozen schema's own constructor already defaults (e.g. `synchronization_strategy` defaulting to `SyncStrategy.NONE`, exactly as the frozen `PlaywrightStep` dataclass itself does).
* **Traceability:** reuses Batch 1's `traceability/*` artifacts conceptually (same REQ↔TC↔DATA↔AUTO chain); this module additionally performs its own defensive, real-time traceability check (requirement-ID match, dataset↔testcase match) at resolution time, independent of whatever traceability snapshot Batch 1 may have persisted separately.

## D. CP-06 Integration

* **Persisted input path:** `resolve_persisted_execution_candidate(automation_id, expected_test_data_set_id=None, execution_id=None)` → `{"ready": bool, "stages": {...}, "governed_artifact", "governed_dataset", "early_result"}`.
* **Execution path:** `execute_persisted_candidate(...)` calls the resolver, then — only if `ready`— calls the real, unmodified `execution.pipeline.run_cp_mvp2_06(governed_artifact, governed_dataset, sut_base_url, ...)`. If not ready, the resolver's own honest `early_result` (`NOT_EXECUTED` or `BLOCKED`, using only the frozen `OverallStatus`/`FailureCategory` vocabulary) is used instead — never a fabricated result.
* **Result persistence:** every result (real-execution or early) is persisted through Batch 1's unmodified, additive `execution/persist.py::persist_execution_result` — the new `runs/YYYY/MM/DD/<execution_id>/` path. The frozen `execution/evidence.py`'s `runs/cp_mvp2_06/<execution_id>/` legacy path is untouched and still populated automatically whenever `run_cp_mvp2_06()` itself performs real browser work.
* **Summary persistence:** `run_batch2_persisted_execution(sut_base_url, batch_id)` — the batch-level runner — builds and persists one `execution/summary.py`-shaped Execution Summary (unchanged Batch 1 module) covering every candidate examined.

## E. Tests

* **New tests:** 25, in `tests/test_batch2_persisted_lifecycle.py` — covering persistence loading (valid/missing/malformed/deserialization-failure/collision-detected/collision-resolved/collision-unresolvable/missing-testcase/missing-test-data/requirement-mismatch/broken-traceability/unsupported/possible-duplicate), CP-06 integration (mocked `BrowserExecutor`, patched at its real frozen import site `execution.pipeline.BrowserExecutor`, exactly mirroring Batch 1's own CP-06 test convention — ineligible-never-reaches-browser, accepted-reaches-browser, possible-duplicate-reaches-browser, collision-blocked-never-reaches-browser, result persistence, field-fidelity roundtrip), traceability (full REQ/TC/DATA chain on the `ExecutionResult`), timestamping (real wall-clock date directory, no fabrication), backward compatibility (frozen `execution.evidence.RUNS_DIR` never reassigned), and the batch-level runner (every distinct collision variant examined separately; honest reporting when nothing is eligible).
* **CP-06 tests (frozen, Batch 1):** `tests/test_cp_mvp2_06_execution.py` — all 38 still pass unmodified.
* **Full regression:** `.venv/Scripts/python.exe -m pytest -q` → **274 passed, 0 failed** (249 pre-existing + 25 new). Run once before implementation (249/0, confirming the stated Batch 1 baseline) and once after (274/0).

## F. Real Execution

Two genuinely distinct pieces of real evidence were produced — clearly distinguished, never conflated:

### F.1 Real-corpus attempt (the actual persisted Batch 1 automation artifacts)

`run_batch2_persisted_execution("https://demowebshop.tricentis.com/", "BATCH2-LIVE-01")` was run against the real, on-disk `automation/generated/` tree (9 unique persisted `automation_id`s, produced by Batch 1's own live validation, still present on disk — not regenerated for this task, per sec. 22).

* Automation IDs examined: `PW-TC-REQ-ACO-03-01-01`, `PW-TC-REQ-ACO-03-02-01`, `PW-TC-REQ-CART-03-01-01`, `PW-TC-REQ-CART-03-02-01`, `PW-TC-REQ-CART-03-03-01`, `PW-TC-REQ-PAY-01-01-01`, `PW-TC-REQ-PAY-01-02-01`, `PW-TC-REQ-REG-01-01-01`, `PW-TC-REQ-REG-01-02-01` (9 total).
* Candidates examined: **22** (every distinct `test_data_set_id` variant under each ID was enumerated and attempted separately — 6 variants for `PW-TC-REQ-REG-01-01-01` alone, a real, on-disk instance of `AUTOMATION_ID_COLLISION_ADVISORY`, see §H).
* By governance status: `{"UNSUPPORTED_NEEDS_CLARIFICATION": 22}`.
* Final result for all 22: **`NOT_EXECUTED`**, note `INELIGIBLE: {'governance_status': 'UNSUPPORTED_NEEDS_CLARIFICATION'}` — the frozen `execution.validate.check_eligibility` gate correctly refused all 22 before any browser was instantiated (verified structurally: this gate runs before `BrowserExecutor` is ever constructed).
* Real, current timestamps (`2026-09-14`), real persisted paths under `runs/2026/09/14/BATCH2-LIVE-01-<...>/`, real persisted summary at `reports/execution_summaries/BATCH2-LIVE-01/v1.json`.
* **This is the honest, correct governed outcome per sec. 21 — not an implementation failure.** It reconfirms, via this task's own independent evidence, the same finding already established at CP-05/CP-06/Batch 1.

### F.2 Disclosed synthetic mechanism check (proves the wiring itself, real browser, real SUT)

Per sec. 20 ("prove that persisted lifecycle artifact → governed CP-06 execution actually works"), and mirroring this project's own established precedent (CP-05's `test_valid_artifact_reaches_accepted_with_synthetic_unambiguous_evidence`; CP-06's own `EXEC-PW-TEST-01-01`), one clearly-labeled, disclosed-synthetic fixture set was persisted (IDs `TC-MECHANISM-CHECK-01` / `TD-MECHANISM-CHECK-01` / `PW-MECHANISM-CHECK-01`, `source_attribution` explicitly noting "DISCLOSED SYNTHETIC MECHANISM FIXTURE") and executed through the **exact same, unmodified** `execute_persisted_candidate()` path — no artifact was manufactured to fake real-corpus eligibility; this is a distinct, honestly-labeled mechanism proof, never presented as corpus evidence.

| Field | Value |
|---|---|
| Execution ID | `BATCH2-MECHANISM-CHECK-01` |
| Automation ID | `PW-MECHANISM-CHECK-01` (disclosed synthetic) |
| Testcase ID | `TC-MECHANISM-CHECK-01` (disclosed synthetic) |
| Requirement ID | `REQ-WISH-01` (real, approved, non-state-mutating) |
| Test-data ID | `TD-MECHANISM-CHECK-01` (disclosed synthetic) |
| Browser | `chromium/151.0.7922.34` (real) |
| Timestamp | `2026-09-14T19:06:00.225440+00:00` → `...:07.335972+00:00` (real, 7.11s elapsed) |
| Final result | **`PASS`** |
| Evidence (legacy path) | `runs/cp_mvp2_06/BATCH2-MECHANISM-CHECK-01/execution.log` (real file, verified on disk) |
| Evidence (new additive path) | `runs/2026/09/14/BATCH2-MECHANISM-CHECK-01/{final_result,step_results,assertions}.json` (real files, verified on disk) |
| All 10 resolution stages | passed |

This conclusively demonstrates the persisted-lifecycle → real-browser wiring works end-to-end, and that both the legacy and new evidence paths coexist without ambiguity, per sec. 10.

## G. Coverage

Clearly separated, per sec. 11/§14's own requirement:

* **Execution coverage:** 0 of 22 real-corpus candidates executed to a real browser step (all 22 correctly `NOT_EXECUTED` at the design-time eligibility gate); the mechanism check's 1 candidate did execute (`PASS`).
* **Testcase coverage:** 9 distinct testcase IDs represented in the real-corpus run.
* **Requirement coverage:** 4 requirements represented (`REQ-REG-01`, `REQ-ACO-03`, `REQ-PAY-01`, `REQ-CART-03`); **0** counted as covered, because coverage here is defined strictly as "at least one PASS execution" (per Batch 1's `execution/summary.py`, unchanged) — never inflated merely because artifacts exist. **100% coverage is not claimed anywhere in this report.**

## H. Governance Findings

* **DATA-OQ-01:** unaffected and unweakened. The frozen `execution.validate.assess_data_oq_01` gate remains exactly as CP-06 froze it; this task did not touch it. It was not exercised against a real eligible corpus artifact this task (none exists), but the mechanism check deliberately used `REQ-WISH-01` — a real, approved, non-state-mutating requirement — specifically so as not to require, weaken, or bypass this gate to obtain a real `PASS`.
* **`AUTOMATION_ID_COLLISION_ADVISORY`:** re-confirmed, with new precision, using real on-disk data this task independently verified: `PW-TC-REQ-REG-01-01-01` has 6 distinct persisted `test_data_set_id` values; `PW-TC-REQ-REG-01-02-01` has 4; `PW-TC-REQ-ACO-03-01-01`/`-02-01` and `PW-TC-REQ-CART-03-{01,02,03}-01` each have 2; `PW-TC-REQ-PAY-01-{01,02}-01` have 1 each (no collision). This task's new resolver **detects this deterministically, blocks an ambiguous request, and never picks a winner** — confirmed by both the unit tests (§E) and the real-corpus run (§F.1, where the batch-level runner explicitly enumerated and attempted every one of the 22 real variants rather than silently collapsing them). **Not fixed here** — frozen `automation/generate.py` was not touched, per sec. 13.
* **No other genuine finding.** No frozen artifact required modification; no STOP condition was triggered.

## I. Backward Compatibility

* `execution/{schema,validate,evidence,engine,pipeline}.py` are byte-for-byte unchanged (`git diff` empty, §A).
* `tests/test_cp_mvp2_06_execution.py` (Batch 1's own 38 CP-06 tests) pass unmodified — verified as part of the 274-passed full regression.
* The legacy `runs/cp_mvp2_06/<execution_id>/` path was real, populated, and verified on disk for the mechanism check (§F.2) in the exact same execution that also populated the new `runs/YYYY/MM/DD/<execution_id>/` path — both coexist without deleting, renaming, or rewriting either.
* `tests/test_batch2_persisted_lifecycle.py::test_persisted_lifecycle_never_touches_frozen_evidence_default` asserts `execution.evidence.RUNS_DIR` is never reassigned by this module.

## J. Final Assessment

```text
PASS WITH ADVISORIES
```

Rationale: all 25 new tests pass; full regression is green (274/0); no frozen artifact was modified; the real-corpus execution attempt produced the honest, governed `NOT_EXECUTED` outcome (a pre-existing, independently re-confirmed finding, not an implementation defect); a genuine, disclosed-synthetic mechanism check proved the real-browser wiring end-to-end with a real `PASS`; both advisories (`AUTOMATION_ID_COLLISION_ADVISORY`, and the carried-forward `DATA-OQ-01`/no-eligible-corpus-artifact condition) are disclosed, not hidden, and neither was worked around.

**This report does not claim Human + Di approval.** Readiness for the Human + Di final gate is stated in the companion summary and in the final response to the human; the gate decision itself remains Human + Di's independent responsibility.

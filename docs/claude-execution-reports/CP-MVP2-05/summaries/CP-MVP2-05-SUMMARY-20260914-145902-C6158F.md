Task: CP-MVP2-05 — Specification Creation & Governance Review (SPECIFICATION ONLY, no implementation)

Specification created: docs/CP-MVP2-05-SPECIFICATION-v1.0.md
Status: DRAFT — pending Human + Di review. NOT approved. NOT frozen.

Grounding: drawn from the frozen CP-MVP2-03/04 specifications (governance/vocabulary/schema precedent) and, distinctively for this checkpoint, from DIRECT INSPECTION of the actual raw HTML captures already produced by CP-MVP2-01 discovery (knowledge/historical/discovery_evidence/captures/*.html) — not assumed or invented.

Key real findings grounding the spec:
- checkout_00.html's Billing/Shipping address form uses a stable, deterministic locator pattern (ASP.NET MVC model-binding IDs, e.g. id="BillingNewAddress_FirstName") — cited as the evidenced-good example.
- cart_with_item.html's quantity control uses name="itemquantity7079994" — an item-specific, non-generalizable literal — cited as the evidenced-fragile example.
- No registration-page (/register) capture exists anywhere in discovery evidence — REQ-REG-01 (which already has an accepted CP-03 testcase and CP-04 dataset) therefore has ZERO locator evidence today; disclosed as an explicit, real limitation rather than papered over.
- Historical discovery captures are confirmed (via knowledge/lib/validate.py's check_historical_vs_approved_conflict) to be deliberately excluded from the queryable RAG KnowledgeBase — the spec requires any future implementation to read these files directly from disk, never via KnowledgeBase, and never conflate their authority tier with Approved-SRS-tier evidence.

Key design decisions: three-level schema (PlaywrightArtifact/PlaywrightStep/LocatorSpec+Assertion) extending CP-04's two-level precedent, justified by the sequential nature of a Playwright scenario; artifact-level (not step-level) acceptance with a "load-bearing step" rule; assertion classification (BUSINESS_REQUIRED/TECHNICAL_USEFUL/DIAGNOSTIC_OPTIONAL) so only business-required assertions count toward coverage; reused (not reinvented) governance vocabulary.

Explicit out-of-scope: CP-MVP2-06 (real browser execution), CP-07 (RCA/replanning/self-healing), CP-08 (performance), CP-09 (reporting), CI/CD, multi-browser execution (Approved SRS discloses headless-Chromium-only discovery).

Open questions carried into the spec (sec. 20): REQ-REG-01 locator gap; unconfirmed payment-method option locators; unresolved cart-quantity locator generalization; inherited unresolved DATA-OQ-01; unvalidated per-step AJAX sync strategy.

Frozen artifacts verified unchanged: CP-MVP2-01, CP-MVP2-02, CP-MVP2-03 specification/implementation, CP-MVP2-04 specification/implementation/freeze checkpoint, Approved SRS — all confirmed via empty git diff, before and after.

No CP-MVP2-05 implementation created. No CP-MVP2-06+ artifact created. No Playwright code, browser execution, or locator implementation created.

Regression: 122 passed, 0 failed (before and after, identical — no code exists to test).

Files changed: docs/CP-MVP2-05-SPECIFICATION-v1.0.md (new), this execution report and summary (new). Nothing else.

Governance findings/advisories: none blocking. The REQ-REG-01 locator-evidence gap and the fragile cart-quantity selector are disclosed as known limitations for Human + Di's awareness, not as defects in this specification task.

RECOMMENDED NEXT ACTION:
Human + Di review of docs/CP-MVP2-05-SPECIFICATION-v1.0.md. Implementation must not begin until this specification is explicitly approved and frozen through the same governance process used for CP-MVP2-03/04.

FINAL STATUS:
CP-MVP2-05 SPECIFICATION PREPARED — DRAFT — PENDING HUMAN + DI REVIEW

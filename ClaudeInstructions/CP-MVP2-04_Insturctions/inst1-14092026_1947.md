# CP-MVP2-04 — LLM Test Data Generation — IMPLEMENTATION

You are now explicitly authorized to implement CP-MVP2-04 against the frozen specification:

`docs/CP-MVP2-04-SPECIFICATION-v1.0.md`

The specification is **APPROVED and FROZEN**.

Freeze commit:

`8271a9a8264c9212dfb5bd784aa0434d5b84a11b`

## GOVERNING RULE

The frozen CP-MVP2-04 specification is the sole implementation contract.

**DO NOT modify the specification.**

**NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

If an implementation problem cannot be reconciled with the frozen specification:

**STOP → REPORT → DO NOT PATCH THE SPECIFICATION.**

A specification change requires a formal Change Request and separate approval.

---

# Objective

Implement the complete CP-MVP2-04 pipeline:

`APPROVED SRS
 → MVP2 KNOWLEDGE BASE
 → GOVERNED RAG
 → ACCEPTED CP-MVP2-03 TESTCASE
 → TEST-DATA REQUIREMENT IDENTIFICATION
 → CLAUDE LLM TEST-DATA GENERATION
 → STRUCTURED TEST DATA
 → DETERMINISTIC VALIDATION
 → UNIQUENESS / DUPLICATE / COVERAGE / GOVERNANCE
 → CP-MVP2-04 RESULT`

The implementation must integrate with the existing frozen MVP2 architecture.

Do NOT reinvent the existing RAG/LLM/testcase infrastructure.

Reuse existing generic components wherever the frozen specification permits.

---

# 1. Inspect before implementation

Before changing anything:

1. Verify current HEAD.
2. Verify `HEAD == origin/main`.
3. Verify working tree state.
4. Read the complete frozen CP-MVP2-04 specification.
5. Inspect existing CP-MVP2-02 Knowledge Base/RAG implementation.
6. Inspect existing CP-MVP2-03:

   * `llm/client.py`
   * `testcases/schema.py`
   * `testcases/generate.py`
   * `testcases/validate.py`
   * `testcases/pipeline.py`
   * relevant tests
7. Understand the existing Claude Pro provider adapter.
8. Confirm the existing 67-test baseline.

Do not modify anything during this inspection phase.

---

# 2. Implementation boundary

Implement only CP-MVP2-04.

Expected implementation areas may include, as appropriate:

* test-data schema/models
* test-data generation
* deterministic validation
* uniqueness/duplicate detection
* coverage
* governance
* CP-04 pipeline
* CP-04 tests
* CP-04 documentation/evidence

Use the existing project structure and naming conventions.

Do not create unnecessary frameworks.

Do not refactor unrelated CP-01/02/03 code.

---

# 3. Test-data schema

Implement the frozen specification's two-level model:

`TestDataSet`
→ `TestDataField`

Support the required fields and fixed vocabularies from the specification.

Important:

* `data_set_id` must be deterministic-code generated.
* LLM must NOT assign governance IDs.
* `requirement_ids` must come from the accepted testcase.
* source attribution must be built deterministically.
* validation status must be deterministic.
* LLM reasoning must never become the governance authority.

---

# 4. LLM integration

Reuse the existing `LLMClient` abstraction and Claude Pro provider path.

The architecture must remain:

`CP-04 pipeline → LLMClient → ClaudeLLMClient → claude -p → authenticated Claude Pro`

Do NOT introduce OpenAI API dependencies.

Do NOT require an OpenAI API key.

Do NOT install a new SDK unless the frozen specification explicitly requires it.

The LLM prompt must provide governed requirement/testcase evidence and request only the reasoning/data proposal required by the frozen schema.

Do not allow the LLM to redefine:

* requirement IDs
* source attribution
* governance status
* coverage
* uniqueness authority

---

# 5. Requirement-derived constraints

Implement deterministic handling for constraints explicitly supported by the Approved SRS/RAG evidence.

Examples include:

* mandatory/optional fields
* email format where evidenced
* unique registration email
* password/confirm-password relationship
* documented allowed values
* documented dependencies
* documented invalid cases
* documented boundaries

Do NOT invent:

* field lengths
* country-code standards
* Shipping Method enumerations not established by the SRS
* undocumented ranges
* undocumented business rules

Where evidence is insufficient, produce:

`UNSUPPORTED_NEEDS_CLARIFICATION`

with an explicit reason.

---

# 6. Uniqueness and duplicates

Implement the frozen uniqueness model.

Support:

* `MUST_BE_UNIQUE`
* `MAY_REUSE`
* `UNIQUENESS_UNKNOWN`

Support deterministic statuses:

* `UNIQUE`
* `DUPLICATE`
* `POSSIBLE_DUPLICATE`

Do not aggressively delete possible duplicates.

Do not alter generated data merely to make a testcase pass when doing so would violate the requirement.

In particular, registration email uniqueness must be governed from the Approved SRS evidence.

---

# 7. Data categories

Implement the frozen categories:

* POSITIVE
* ALTERNATE
* EXCEPTIONAL
* BOUNDARY
* INVALID

Do not manufacture categories merely to increase apparent coverage.

Boundary generation must be evidence-driven.

If no boundary is documented:

`UNSUPPORTED_NEEDS_CLARIFICATION`

rather than an invented boundary.

---

# 8. Traceability

Every accepted dataset must preserve:

`DATASET
 → TESTCASE
 → REQUIREMENT
 → RAG EVIDENCE
 → APPROVED SRS`

Build attribution deterministically from the same governed evidence used by CP-03.

No fabricated source/chunk IDs.

No LLM-generated attribution accepted as authoritative.

---

# 9. Coverage

Implement deterministic data coverage according to the frozen specification.

Coverage must not be inflated by:

* invalid datasets
* rejected datasets
* quarantined datasets
* orphan datasets
* unknown requirement IDs
* fabricated attribution

Be particularly careful with `POSSIBLE_DUPLICATE`.

It must not silently inflate meaningful business coverage if the specification's governance semantics do not permit that.

Add explicit tests for this behavior.

---

# 10. Unsupported / clarification behavior

Implement deterministic handling for:

* missing evidence
* missing dependency
* unresolved allowed values
* undocumented boundary
* ambiguous requirement
* unresolved DATA-OQ-01
* conflicting approved information

Never silently substitute plausible LLM knowledge.

---

# 11. DATA-OQ-01

Do NOT resolve:

`DATA-OQ-01 — shared-instance data-reset/isolation policy`

inside the implementation.

If a generated dataset requires an assumption about shared-instance isolation/reset that is not established by governed evidence:

return the governed unsupported/clarification result required by the specification.

Do not invent an environment policy.

---

# 12. Deterministic governance

The implementation must follow:

**LLM proposes. Deterministic code decides.**

Deterministic validators must govern at minimum:

* schema
* required fields
* testcase linkage
* requirement linkage
* source attribution
* constraint conformance
* data category validity
* boundary validity
* uniqueness
* duplicates
* coverage
* unsupported status

---

# 13. Testing

Create comprehensive CP-MVP2-04 tests.

At minimum test:

### Schema

* valid dataset
* invalid dataset
* required fields
* invalid value types
* fixed vocabularies

### Traceability

* valid testcase
* missing testcase
* unknown requirement
* attribution preservation
* orphan dataset

### Constraint validation

* mandatory field
* optional field
* email format
* password confirmation
* documented allowed values
* undocumented values
* undocumented boundary

### Categories

* POSITIVE
* ALTERNATE
* EXCEPTIONAL
* BOUNDARY
* INVALID

### Uniqueness

* unique email
* duplicate email
* possible duplicate
* reusable invalid email
* unknown uniqueness

### Unsupported

* missing evidence
* missing dependency
* DATA-OQ-01
* unresolved Shipping Method
* unresolved Country value set
* undocumented boundary

### Coverage

* accepted dataset counts
* rejected dataset does not count
* quarantined dataset does not count
* orphan does not count
* duplicate behavior
* possible-duplicate behavior

### LLM provider

Mock/stub provider tests must verify deterministic behavior without requiring a live Claude invocation for every test.

---

# 14. Live Claude verification

After deterministic implementation and tests are complete:

First establish:

`67 + CP-04 tests = new baseline`

Then perform a controlled live Claude verification using the existing Claude Pro provider.

Do not retry blindly.

Use real governed CP-02/CP-03 evidence.

Verify:

* Claude invocation succeeds
* response is parseable
* structured data is produced
* deterministic validators process it
* attribution is deterministic
* governance works
* coverage is correct

If the live provider exhibits variability, document it rather than weakening deterministic governance.

---

# 15. Regression

Run:

1. CP-04 tests independently.
2. Full existing regression.
3. Full regression after implementation.
4. Any controlled live smoke/verification required by the evidence protocol.

Record exact commands and results.

Existing CP-01/02/03 behavior must remain intact.

---

# 16. Frozen artifact protection

Before and after implementation verify that these remain unchanged:

* CP-MVP2-01 artifacts
* CP-MVP2-02 artifacts
* `docs/CP-MVP2-03-SPECIFICATION-v1.0.md`
* `docs/CP-MVP2-03-CLAUDE-PROVIDER-DECISION.md`
* CP-MVP2-03 implementation
* Approved SRS
* `docs/CP-MVP2-04-SPECIFICATION-v1.0.md`

If any changes unexpectedly:

**STOP → REPORT → DO NOT REPAIR SILENTLY.**

---

# 17. Evidence/reporting

Create detailed execution evidence under:

`docs/claude-execution-reports/CP-MVP2-04/`

and a corresponding summary under:

`docs/claude-execution-reports/CP-MVP2-04/summaries/`

Document:

* starting commit
* specification commit
* files inspected
* files added
* files modified
* architecture implemented
* tests
* regression
* live Claude verification
* governance results
* coverage results
* limitations
* known advisories
* ending commit
* repository status
* final implementation status

---

# 18. Git discipline

Do not amend previous commits.

Create new implementation commit(s).

Use clear commit messages.

Push to `origin/main`.

Verify final:

`HEAD == origin/main`

and report the final commit SHA.

Do not commit unrelated files.

Do not silently repair the previously disclosed external `ClaudeInstructions` relocation.

---

# 19. Mandatory final response

Your final response MUST provide actual clickable GitHub URLs, not merely repository-relative paths.

Provide:

1. **Implementation report:** full GitHub URL
2. **Implementation summary:** full GitHub URL
3. **Implementation documentation:** full GitHub URL
4. **Relevant test file(s):** full GitHub URL(s)
5. **Final implementation commit:** full GitHub commit URL
6. **Final commit SHA**
7. **CP-04 test result**
8. **Full regression result**
9. **Live Claude verification result**
10. **Coverage result**
11. **Governance result**
12. **Frozen artifact verification**
13. **Known limitations/advisories**
14. **Final status**

If a report URL cannot be provided, do NOT claim completion.

---

# STOP CONDITIONS

STOP immediately if:

* frozen specification changes
* Approved SRS changes
* CP-01/02/03 artifacts change unexpectedly
* implementation requires undocumented business rules
* implementation requires changing response/governance semantics in the frozen specification
* Claude proposes fabricated requirements/constraints
* attribution cannot be established
* deterministic governance cannot establish compliance

In every such case:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not proceed to CP-MVP2-05.

## Final target

Implement CP-MVP2-04 completely and produce evidence sufficient for Human + Di to issue the:

**CP-MVP2-04 IMPLEMENTATION GATE**

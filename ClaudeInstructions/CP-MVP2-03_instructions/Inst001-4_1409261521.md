# TASK — CP-MVP2-02 Evidence Persistence & Repository Archival

## 1. Task Identity

**Task ID:** `TASK-CP-MVP2-02-EVIDENCE-PERSIST`

**Purpose:**
Verify and formally persist the two existing project evidence/instruction artifacts into the MVP2 Git repository, without changing their substantive content and without performing any CP-MVP2-03 implementation.

**Repository:**
`VaijanathR/agentic-qe-mvp2`

**Local repository path:**
`D:\AI PROJECTS\agentic-qe-mvp2`

**Branch:**
`main`

---

# 2. STRICT SCOPE

This is a **repository evidence persistence / archival task only**.

You MAY:

* inspect the current repository state;
* inspect the two specified documents;
* verify their contents and provenance;
* verify whether they are already committed;
* verify whether they are untracked locally;
* verify whether the GitHub `main` branch already contains them;
* commit them if they are genuinely present locally but not committed;
* push the commit if required;
* create a concise execution evidence report documenting the result.

You MUST NOT:

* implement CP-MVP2-03;
* modify the frozen CP-MVP2-03 specification;
* modify CP-MVP2-01;
* modify CP-MVP2-02 implementation;
* modify the Approved SRS;
* modify the Draft SRS;
* modify tests or application code;
* create CP-MVP2-03 testcase-generation code;
* create LLM integration;
* create downstream CP-MVP2-04 artifacts;
* change governance rules;
* silently rewrite either document;
* "clean up" unrelated working-tree changes;
* commit unrelated files merely because they happen to be present.

## Golden Rule

**DO NOT CHANGE ANY SPECIFICATION, REQUIREMENT, GOVERNANCE RULE, OR HISTORICAL EVIDENCE TO MAKE THE REPOSITORY STATE LOOK CLEAN.**

---

# 3. DOCUMENTS TO VERIFY

Verify these two artifacts specifically.

### Document 1

`docs/CP-MVP2-02-GIT-CHECKPOINT.md`

Purpose:

Historical checkpoint evidence for the completed/frozen CP-MVP2-02 checkpoint.

Expected characteristics include evidence relating to:

* CP-MVP2-02 completion;
* Git state;
* commit `e85fd361498a293d5b0430bceb6464f7251649f4`;
* 25/25 tests;
* 12/12 deterministic governance checks;
* Approved/Draft SRS integrity;
* confirmation that CP-MVP2-03 had not started;
* CP-MVP2-02 gate decision.

Do NOT rewrite it merely for formatting or style.

---

### Document 2

`ClaudeInstructions/Inst001-3_1409261402.md`

Purpose:

Historical Claude instruction/evidence artifact associated with the project workflow.

Inspect it and preserve its existing contents.

Do NOT rewrite, summarize over, replace, or improve the document unless absolutely required for repository integrity.

---

# 4. FIRST ACTION — INSPECT CURRENT GIT STATE

Before doing anything else, inspect:

```text
git status
git branch --show-current
git log --oneline -10
git remote -v
```

Then determine:

1. Are either/both documents already tracked?
2. Are either/both documents already committed?
3. Are either/both documents already present on `origin/main`?
4. Does the local working tree contain versions that differ from `origin/main`?
5. Are there unrelated pre-existing modifications?

## IMPORTANT

The Human Owner has stated that the two documents were uploaded directly to the GitHub `main` branch.

Therefore:

**DO NOT ASSUME THEY ARE STILL UNTRACKED.**

Verify the actual Git/GitHub state first.

If they are already committed and pushed:

> Do NOT create another commit.

Instead, verify and report that they are already persisted.

If they are present locally but genuinely untracked/uncommitted:

> Proceed with the controlled commit described below.

If they exist both locally and remotely but differ:

> STOP.

Do not overwrite either version.

Report the discrepancy for Human + Di review.

---

# 5. CONTENT INTEGRITY CHECK

For each document:

1. Read the complete file.
2. Confirm the filename and path.
3. Confirm it is the intended artifact.
4. Compare the local version with the GitHub `main` version if it already exists remotely.
5. If the versions match:

   * PASS.
6. If they differ:

   * STOP.
   * do not reconcile automatically.
   * report exact discrepancy.

Do NOT normalize whitespace, headings, wording, timestamps, hashes, or formatting unless required to resolve a true repository corruption issue.

Historical evidence should remain historical evidence.

---

# 6. CP-MVP2-02 INTEGRITY CHECK

Verify that the evidence document does not contradict the known frozen CP-MVP2-02 state.

At minimum verify:

* CP-MVP2-02 implementation remains intact.
* Commit `e85fd361498a293d5b0430bceb6464f7251649f4` remains the CP-MVP2-02 implementation checkpoint.
* Approved SRS remains unchanged.
* Draft SRS remains unchanged.
* CP-MVP2-03 implementation has not been introduced by this task.
* No downstream CP-MVP2-04 implementation has been introduced.

Run the existing test suite if practical and record the result.

Do not modify code or tests to obtain a passing result.

---

# 7. CP-MVP2-03 INTEGRITY CHECK

This task must NOT implement CP-MVP2-03.

Verify that:

* `docs/CP-MVP2-03-SPECIFICATION-v1.0.md` remains unchanged.
* CP-MVP2-03 preparation/evidence artifacts remain unchanged.
* No testcase-generation implementation has been introduced.
* No LLM testcase-generation code has been introduced.
* No CP-MVP2-04 work has been introduced.

If unexpected CP-MVP2-03 implementation artifacts are found:

> STOP → REPORT → HUMAN + DI DECIDE.

Do not remove them automatically.

---

# 8. UNRELATED FILES

There may be other pre-existing working-tree changes.

Do NOT:

* clean them;
* reset them;
* delete them;
* stage them;
* include them in the commit.

In particular, do not automatically include:

* `.gitignore` changes;
* `README.md` changes;
* unrelated Claude instruction files;
* unrelated generated files;
* unrelated project artifacts.

Only the two specified documents are in scope.

---

# 9. COMMIT DECISION

After inspection:

### Case A — Already committed and pushed

If both documents are already present on `origin/main` and match the intended local versions:

**Do NOT create a new commit.**

Record:

```text
Already persisted: YES
New commit required: NO
Reason: artifacts already exist on origin/main
```

This is a successful outcome.

---

### Case B — Locally present but not committed

If either document is genuinely untracked/uncommitted and matches the intended artifact:

Stage ONLY the specified document(s).

Verify:

```text
git diff --cached --stat
git diff --cached --name-only
```

The staged file list must contain ONLY:

```text
docs/CP-MVP2-02-GIT-CHECKPOINT.md
ClaudeInstructions/Inst001-3_1409261402.md
```

or only whichever of those is genuinely not yet committed.

Then commit using:

```text
docs: persist CP-MVP2-02 checkpoint and execution evidence
```

Push to:

```text
origin/main
```

---

### Case C — Content mismatch

If a local document differs from the GitHub version:

**STOP.**

Do not overwrite either version.

Report:

* local version;
* remote version;
* nature of discrepancy;
* hashes if available;
* Human + Di decision required.

---

# 10. POST-COMMIT / POST-PUSH VERIFICATION

If a commit is required:

Verify:

```text
git status
git log -1 --oneline
git rev-parse HEAD
git rev-parse origin/main
```

Confirm:

```text
HEAD == origin/main
```

Confirm the commit contains only the intended files.

If push fails:

> STOP → REPORT → HUMAN + DI DECIDE.

Do not retry destructive Git operations.

---

# 11. EXECUTION EVIDENCE REPORT

Create:

```text
docs/claude-execution-reports/CP-MVP2-02-EVIDENCE-PERSIST/
```

Create one detailed report:

```text
CP-MVP2-02-EVIDENCE-PERSIST-EXEC-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

The report must contain:

1. Task identity.
2. Execution ID.
3. Repository.
4. Branch.
5. Initial Git state.
6. Documents inspected.
7. Document paths.
8. Content-integrity results.
9. CP-MVP2-02 integrity result.
10. CP-MVP2-03 integrity result.
11. Test result, if executed.
12. Files staged, if any.
13. Commit SHA, if a commit was required.
14. Push result, if a push was required.
15. Final Git state.
16. Any pre-existing unrelated changes.
17. Any deviations.
18. Any problems.
19. Final result.

---

# 12. DI REVIEW SUMMARY

Also create:

```text
docs/claude-execution-reports/CP-MVP2-02-EVIDENCE-PERSIST/summaries/
```

with:

```text
CP-MVP2-02-EVIDENCE-PERSIST-SUMMARY-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

The summary should be concise and specifically suitable for Human + Di checkpoint review.

It must clearly state:

```text
RESULT: PASS / BLOCKED
DOCUMENT 1: verified
DOCUMENT 2: verified
CP-MVP2-02: unchanged
CP-MVP2-03: not implemented
SRS: unchanged
COMMIT: <SHA or ALREADY PERSISTED>
PUSH: SUCCESS / NOT REQUIRED / FAILED
FINAL STATUS: <status>
```

---

# 13. IMPORTANT REPORTING RULE

Both reports must be:

1. created locally;
2. verified;
3. committed if a new commit is required;
4. pushed if a new commit is required;
5. referenced in the final Claude response.

If the two target documents were already committed before this task, the execution reports themselves should still be persisted in a separate evidence commit.

Do not mix unrelated files into that commit.

---

# 14. FINAL RESPONSE TO HUMAN + DI

Your final response MUST include:

### Execution

* Task ID.
* Execution ID.
* Result.
* Repository.
* Branch.

### Documents

* Document 1 verification result.
* Document 2 verification result.
* Whether each was already committed or required persistence.

### Integrity

* CP-MVP2-02 unchanged.
* CP-MVP2-03 unchanged/not implemented.
* Approved SRS unchanged.
* Draft SRS unchanged.

### Git

* Existing commit, if already persisted.
* New commit SHA, if created.
* Push result.
* Final HEAD.
* Final origin/main.
* Working-tree state.

### Evidence

* Detailed execution report path.
* Di review summary path.

### Decision

Clearly state whether any Human + Di decision is required.

---

# 15. HARD STOP CONDITIONS

STOP immediately if:

* either target document has conflicting local/remote versions;
* the frozen CP-MVP2-02 implementation appears altered unexpectedly;
* Approved SRS changed unexpectedly;
* Draft SRS changed unexpectedly;
* CP-MVP2-03 implementation appears unexpectedly;
* unrelated files would need to be changed;
* Git state is ambiguous;
* push fails;
* resolving the task requires changing historical evidence;
* resolving the task requires changing a frozen specification.

For any such case:

**STOP → REPORT → HUMAN + DI DECIDE.**

---

# 16. FINAL SCOPE BOUNDARY

This task is ONLY:

```text
VERIFY
   ↓
PRESERVE
   ↓
PERSIST
   ↓
VERIFY GIT
   ↓
REPORT
```

It is NOT:

```text
CP-MVP2-03 IMPLEMENTATION
```

CP-MVP2-03 implementation remains a separate, explicitly authorized task.

---

## Golden Rule

**NEVER CHANGE THE SPECIFICATION, REQUIREMENT, GOVERNANCE RULE, OR HISTORICAL EVIDENCE TO MAKE THE IMPLEMENTATION OR REPOSITORY STATE PASS.**

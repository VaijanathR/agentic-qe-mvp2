# TASK-CP-MVP2-03-LIVE-LLM-SMOKE

## CP-MVP2-03 — Controlled Live LLM Smoke Verification

---

## 1. TASK AUTHORIZATION

You are authorized to perform a **controlled live-LLM smoke verification** for the already implemented **CP-MVP2-03 — LLM Testcase Generation** capability.

This is a **verification-only task**.

The CP-MVP2-03 specification:

`docs/CP-MVP2-03-SPECIFICATION-v1.0.md`

is already:

> **APPROVED / FROZEN — v1.0**

The CP-MVP2-03 implementation has already passed deterministic and regression testing with:

> **47/47 tests passing**

The only remaining advisory is that the **real LLM/API path has not yet been exercised** because the implementation verification used the deterministic/stub LLM path.

---

# 2. PRIMARY OBJECTIVE

Verify, with **one controlled real LLM invocation**, that the implemented CP-MVP2-03 path works end-to-end:

```text id="x7v2pn"
Approved SRS
      ↓
CP-MVP2-02 Governed RAG
      ↓
Retrieved Approved Requirement
      ↓
REAL LLM
      ↓
Structured JSON Testcase Response
      ↓
Existing CP-MVP2-03 Parser
      ↓
Existing Deterministic Validators
      ↓
Governed Testcase Result
```

The objective is **not** to generate a large testcase suite.

The objective is to prove that the real LLM can successfully participate in the already implemented governed pipeline.

---

# 3. NON-NEGOTIABLE GOVERNANCE

### DO NOT MODIFY THE FROZEN SPECIFICATION.

Do not modify:

`docs/CP-MVP2-03-SPECIFICATION-v1.0.md`

for any reason.

If the live smoke test exposes a conflict between the implementation and the specification:

> **STOP → REPORT → HUMAN + DI DECIDE**

Do NOT change the specification to make the smoke test pass.

---

# 4. DO NOT MODIFY THE IMPLEMENTATION UNLESS EXPLICITLY AUTHORIZED

This task is verification-only.

Do NOT:

* refactor CP-MVP2-03
* redesign the LLM client
* modify the RAG implementation
* modify testcase schema
* modify deterministic validators
* change prompts merely to obtain a passing result
* change governance rules
* modify CP-MVP2-01
* modify CP-MVP2-02
* begin CP-MVP2-04
* introduce new application functionality
* add self-healing
* add browser execution
* add performance testing
* add security testing

If the live smoke test fails:

> **REPORT THE FAILURE FIRST.**

Do not automatically modify the implementation.

---

# 5. FIRST INSPECT CURRENT REPOSITORY STATE

Before doing anything:

1. Inspect Git branch.
2. Inspect HEAD.
3. Inspect `origin/main`.
4. Inspect working-tree status.
5. Confirm the current CP-MVP2-03 implementation commit.
6. Confirm the CP-MVP2-03 specification remains unchanged.
7. Confirm CP-MVP2-01 and CP-MVP2-02 remain unchanged.
8. Confirm Approved and Draft SRS remain unchanged.

Record the starting state.

If the repository state is ambiguous:

> **STOP → REPORT**

---

# 6. VERIFY LLM CONFIGURATION

Inspect the existing CP-MVP2-03 LLM configuration.

Use the already implemented LLM client.

Do NOT create a second LLM integration.

Do NOT replace the existing LLM provider.

Do NOT introduce a new framework.

Confirm:

* provider configuration
* model configuration
* required environment variables
* API-key availability
* existing configuration mechanism

---

# 7. SECURITY OF CREDENTIALS

If the required API key/credential is unavailable:

> **DO NOT fabricate a successful LLM call.**

Report:

```text
LIVE LLM SMOKE TEST: BLOCKED
Reason: required credential unavailable
```

Do not:

* print the API key
* write the API key to a file
* commit the API key
* expose the credential in logs
* include the credential in the execution report

If the environment is configured correctly, proceed.

---

# 8. SELECT ONE APPROVED REQUIREMENT

Use **one real requirement from the existing Approved SRS / governed KB**.

Prefer a simple requirement that:

* is clearly approved
* has strong RAG evidence
* is already supported by CP-MVP2-03
* does not require downstream test data or browser execution
* can reasonably produce one testcase

Record:

* requirement ID
* source document
* source version
* approval status
* retrieval evidence/chunk identifier where available

Do not invent a requirement.

Do not modify the requirement.

Do not use a Draft-only requirement.

---

# 9. USE THE EXISTING GOVERNED RAG PATH

Retrieve the requirement through the existing CP-MVP2-02 governed RAG interface.

Do NOT:

* directly read arbitrary source files and bypass RAG
* manually construct a fake retrieval result
* manually inject requirement text into the LLM path
* alter the retrieved requirement
* alter approval status

The smoke test must prove the real:

> **RAG → LLM**

integration.

---

# 10. ONE CONTROLLED LLM INVOCATION

Perform a minimal real LLM invocation.

The LLM should receive the governed requirement context through the existing CP-MVP2-03 implementation.

Request structured testcase output according to the existing implementation/schema.

Prefer **one testcase** for the smoke test.

Do not request a large batch.

Do not attempt to maximize coverage.

Do not create unnecessary token/cost usage.

The objective is integration verification, not testcase generation scale.

---

# 11. RECORD THE LLM RESPONSE SAFELY

Capture enough evidence to establish that the real LLM responded successfully.

Record:

* provider
* model
* invocation success/failure
* response format
* parser result
* testcase generated/not generated
* deterministic validation result

Do NOT record:

* API key
* authentication headers
* sensitive credentials
* unnecessary private/environment information

If the response contains sensitive information, redact it before recording evidence.

---

# 12. VERIFY STRUCTURED OUTPUT

Confirm that the real LLM response:

1. is returned successfully;
2. is parseable by the existing CP-MVP2-03 parser;
3. conforms to the expected response structure;
4. can proceed into the existing deterministic validation layer.

Do not manually edit the LLM response before validation.

The actual returned response must be what is tested.

---

# 13. VERIFY DETERMINISTIC GOVERNANCE

Pass the real LLM-generated testcase through the existing validators.

Verify:

### Schema

* valid structure

### Requirement

* valid requirement mapping
* requirement exists

### Approval

* requirement is Approved

### Traceability

* source attribution remains valid

### Governance

* testcase can be classified correctly

The LLM's own claims must not override deterministic validation.

---

# 14. IMPORTANT: TESTCASE ID / ATTRIBUTION

Where the existing implementation deterministically assigns:

* testcase ID
* requirement IDs
* source attribution

continue using the existing implementation.

Do not ask the LLM to become the authoritative source for these fields merely to make the smoke test look more realistic.

We are testing the implementation that already exists.

---

# 15. SUCCESS CRITERIA

The live smoke test is successful only if ALL of the following occur:

### A. Retrieval

* Approved requirement retrieved through CP-MVP2-02 RAG.

### B. LLM

* Real LLM/API invocation succeeds.

### C. Response

* LLM returns a usable structured response.

### D. Parsing

* Existing CP-MVP2-03 parser accepts/parses the response.

### E. Governance

* Existing deterministic validators process the generated testcase.

### F. Traceability

* Generated testcase maps to the retrieved approved requirement.

### G. Attribution

* Source attribution remains available.

### H. No fabrication

* No manually fabricated LLM result is used as evidence.

---

# 16. FAILURE CLASSIFICATION

If the smoke test fails, classify the failure before taking any action.

Possible categories include:

```text id="1q5j0a"
CONFIGURATION
CREDENTIAL
NETWORK
PROVIDER/API
MODEL/RESPONSE
PARSING
SCHEMA
RAG
TRACEABILITY
GOVERNANCE
IMPLEMENTATION
ENVIRONMENT
UNKNOWN
```

Provide:

* observed behavior
* expected behavior
* evidence
* likely failure layer
* whether the problem is reproducible

Do not immediately change code.

---

# 17. NO AUTOMATIC FIXES

This is especially important.

If the real LLM returns unexpected JSON, malformed content, unexpected fields, or an unexpected scenario:

**Do not modify the parser/prompt/schema automatically.**

Instead report:

```text id="0v9x6b"
Observed:
Expected:
Evidence:
Likely cause:
Specification impact:
Implementation impact:
Recommended next action:
```

Then stop if an implementation change appears necessary.

Human + Di will decide whether another implementation task is required.

---

# 18. REGRESSION AFTER SMOKE TEST

If **no code or repository files are modified**, rerun the existing regression suite and confirm the baseline remains:

> **47 passed / 0 failed**

If any existing test unexpectedly fails:

> **STOP → REPORT**

Do not repair unrelated tests.

If a file must be modified to make the smoke test work, STOP before modifying it and report the required change.

This task does not authorize implementation changes.

---

# 19. GIT GOVERNANCE

Because this is verification-only:

### Preferred outcome

If no files need modification:

> **Do not create an implementation commit.**

Evidence reports may be created according to Section 20.

Do not commit unrelated changes.

Do not modify frozen artifacts.

Before committing evidence:

```text
git status
git diff
git diff --cached
```

must be reviewed.

---

# 20. REQUIRED DETAILED EXECUTION REPORT

Create:

```text id="d1h9r4"
docs/claude-execution-reports/CP-MVP2-03-LIVE-LLM-SMOKE/
```

with:

```text id="q5x8s1"
CP-MVP2-03-LIVE-LLM-SMOKE-EXEC-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

The report must include:

### Task identity

* task ID
* execution ID
* date/time
* repository
* branch

### Baseline

* starting HEAD
* origin/main
* Git status
* regression result

### Specification

* CP-MVP2-03 v1.0
* confirmation specification was unchanged

### Requirement

* requirement ID
* source
* version
* approval status

### RAG

* retrieval mechanism
* retrieval evidence
* source attribution

### LLM

* provider
* model
* invocation result
* response structure

Never expose credentials.

### Parsing

* parser result

### Deterministic validation

* schema
* traceability
* approval
* attribution
* governance result

### Final smoke result

Use:

```text
PASS
BLOCKED
FAILED
```

### Regression

* final test count
* pass/fail

### Git

* whether any files changed
* commit hash if evidence was committed
* push status
* final HEAD
* origin/main

### Limitations/advisories

Clearly document anything that was not verified.

---

# 21. REQUIRED DI REVIEW SUMMARY

Create:

```text id="p4h1q7"
docs/claude-execution-reports/CP-MVP2-03-LIVE-LLM-SMOKE/summaries/
```

with:

```text id="m8c3v6"
CP-MVP2-03-LIVE-LLM-SMOKE-SUMMARY-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

The summary must be specifically optimized for Di's independent review.

Include:

* execution ID
* requirement tested
* RAG result
* real LLM result
* parser result
* deterministic governance result
* traceability result
* attribution result
* regression result
* Git status
* limitations
* recommended gate

End with:

```text
RECOMMENDED GATE:
PASS / ADVISORY / BLOCKED
```

Do not state that Di has approved the result.

---

# 22. EVIDENCE QUALITY

Evidence must distinguish clearly between:

### Real

Actual LLM response/invocation.

### Deterministic

Code-generated validation/coverage/traceability.

### Synthetic

Any deliberately synthetic test used to exercise a governance branch.

### Not tested

Anything not actually exercised.

Never present synthetic or stub output as live LLM evidence.

---

# 23. IMPORTANT SCOPE BOUNDARY

This task verifies only:

```text id="4a2f6d"
CP-MVP2-02 RAG
       ↓
REAL LLM
       ↓
CP-MVP2-03 TESTCASE
       ↓
DETERMINISTIC VALIDATION
```

It does NOT begin:

* CP-MVP2-04
* test-data generation
* browser automation
* Playwright execution
* API execution
* performance testing
* security testing
* self-healing
* RCA/replanning
* CI/CD
* PR automation

---

# 24. HARD STOP CONDITIONS

Stop immediately if:

1. API credential is unavailable.
2. RAG cannot provide an approved requirement.
3. Real LLM invocation cannot be performed.
4. Specification appears inconsistent with implementation.
5. Implementation modification appears necessary.
6. CP-MVP2-01/02 changes unexpectedly.
7. Approved/Draft SRS changes unexpectedly.
8. Existing tests unexpectedly fail.
9. Git state becomes ambiguous.
10. Evidence would need to be fabricated.
11. Any secret would need to be exposed.
12. A downstream CP would need to be implemented.

For any hard stop:

```text id="m7k4z2"
STOP
↓
PRESERVE CURRENT STATE
↓
RECORD FACTS
↓
DO NOT CHANGE THE SPEC
↓
DO NOT MODIFY IMPLEMENTATION
↓
REPORT
↓
HUMAN + DI DECIDE
```

---

# 25. FINAL RESPONSE

Return a concise response in this structure:

```text
TASK: CP-MVP2-03-LIVE-LLM-SMOKE
EXECUTION ID: <id>

SPECIFICATION:
CP-MVP2-03-SPECIFICATION-v1.0.md
STATUS: FROZEN / UNCHANGED

REQUIREMENT TESTED:
<requirement ID>

RAG:
PASS / FAIL

REAL LLM:
PASS / FAIL / BLOCKED

PARSER:
PASS / FAIL

DETERMINISTIC GOVERNANCE:
PASS / FAIL

TRACEABILITY:
PASS / FAIL

ATTRIBUTION:
PASS / FAIL

REGRESSION:
<result>

IMPLEMENTATION CHANGES:
NONE / <details>

GIT:
<status>

DETAILED REPORT:
<GitHub link>

DI REVIEW SUMMARY:
<GitHub link>

RECOMMENDED GATE:
PASS / ADVISORY / BLOCKED

LIMITATIONS / ADVISORIES:
<details>
```

---

## FINAL GOVERNANCE PRINCIPLE

This is a **verification task, not a repair task**.

The goal is to answer one question:

> **Does the already implemented CP-MVP2-03 pipeline work with a real LLM invocation?**

If YES:

> report evidence and recommend PASS.

If NO:

> report exactly where it failed.

**Do not change the specification.
Do not silently change the implementation.
Do not fabricate success.**

### Golden rule

> **NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

> **If implementation changes appear necessary: STOP → REPORT → HUMAN + DI DECIDE.**

### FINALLY
share the links to the reports created
# CP-MVP2-03 — Live LLM Smoke Verification RETRY

## Objective

Retry the previously BLOCKED CP-MVP2-03 live LLM smoke verification now that the required `OPENAI_API_KEY` is available in the WSL Ubuntu execution environment.

This is a **verification-only task**.

The CP-MVP2-03 specification remains **APPROVED/FROZEN v1.0**.

The existing CP-MVP2-03 implementation remains the implementation under test.

---

# 1. GOVERNANCE — NON-NEGOTIABLE

You MUST NOT:

* modify the frozen CP-MVP2-03 specification
* modify the Approved SRS
* modify the Draft SRS
* modify CP-MVP2-01 artifacts
* modify CP-MVP2-02 artifacts
* modify CP-MVP2-03 implementation code
* add a second LLM integration
* change the LLM client
* change validation logic
* change testcase schema
* change RAG behavior
* change requirements
* change test data
* weaken any validator
* change expected outcomes to make the live result pass
* start CP-MVP2-04
* perform browser automation
* perform performance testing
* perform security testing
* perform self-healing
* perform CI/CD or PR automation

Do NOT "fix" anything during this task.

If the live smoke reveals an implementation defect:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not modify the implementation.

The frozen specification is the source of truth.

**NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

---

# 2. ENVIRONMENT

Execution environment:

* Windows host
* WSL Ubuntu
* Claude Code running inside WSL
* MVP2 repository available locally
* `OPENAI_API_KEY` has been provisioned in the WSL environment

Use the existing environment variable only.

You MUST NOT:

* print the API key
* echo the API key
* include the API key in any report
* include the API key in command output
* write the API key to a file
* commit the API key
* expose the API key in GitHub
* inspect or report the actual credential value

It is sufficient to establish that the variable is available.

---

# 3. STARTING-STATE VERIFICATION

Before executing the smoke test:

1. Record current Git branch.
2. Record current HEAD.
3. Record `origin/main`.
4. Confirm working-tree status.
5. Confirm `OPENAI_API_KEY` is available **without displaying its value**.
6. Confirm the CP-MVP2-03 implementation files are unchanged from the previous verified implementation.
7. Confirm the frozen CP-MVP2-03 specification remains unchanged.
8. Confirm `requirements/` and `knowledge/` remain unchanged.

If unexpected implementation/specification changes are found:

**STOP immediately and report them.**

Do not modify them.

---

# 4. BASELINE REGRESSION

Run the existing full regression suite before the live smoke.

Expected baseline:

```text
47 passed, 0 failed
```

If the baseline is not green:

**STOP.**

Do not modify anything.

Record the actual result.

---

# 5. LIVE LLM SMOKE — EXACT SCOPE

Perform exactly **ONE real LLM generation call**.

Use the existing implementation:

```text
OpenAILLMClient
```

Do NOT use:

```text
StubLLMClient
```

for the live-generation evidence.

The purpose is specifically to verify the real LLM path.

---

# 6. REQUIREMENT

Use the same real Approved requirement selected in the previous blocked smoke:

```text
REQ-REG-01
```

Source:

```text
requirements/MVP2_SRS_v1.0_APPROVED.md
```

Version:

```text
v1.0
```

Approval status:

```text
Approved
```

Retrieve the requirement through the existing CP-MVP2-02 Knowledge Base / governed RAG path.

Do NOT bypass RAG by manually copying the requirement into the LLM prompt.

The evidence must demonstrate:

```text
Approved SRS
    ↓
MVP2 Knowledge Base
    ↓
Governed RAG
    ↓
Retrieved requirement context
    ↓
Real LLM
```

---

# 7. REAL LLM INVOCATION

Use the existing `OpenAILLMClient` implementation and its existing configuration.

Respect:

```text
OPENAI_API_KEY
```

and the existing model configuration:

```text
CP_MVP2_03_LLM_MODEL
```

with its existing default behavior if the variable is not set.

Do not change model configuration.

Do not add configuration.

Do not change code.

Make exactly one real LLM generation request.

Record:

* provider
* model actually used, if safely available
* requirement ID
* source document
* source version
* approval status
* confirmation that the call was actually made
* whether a real response was received
* whether parsing succeeded

Do NOT record:

* API key
* Authorization header
* secrets
* credential values

---

# 8. REAL RESPONSE PARSING

Take the actual response returned by the real LLM and pass it through the existing CP-MVP2-03 response/parsing path.

Do NOT replace the response with:

* StubLLMClient output
* manually created testcase JSON
* hand-edited testcase content
* synthetic live response

The evidence must clearly distinguish:

```text
REAL LLM RESPONSE
```

from:

```text
STUB / SYNTHETIC TEST DATA
```

---

# 9. DETERMINISTIC GOVERNANCE VALIDATION

Pass the actual parsed LLM output through the existing deterministic CP-MVP2-03 validators.

Verify, using the existing implementation:

### Schema

* structured testcase output is accepted/rejected correctly
* required fields are handled correctly

### Traceability

Confirm:

```text
TC
 ↓
REQ-REG-01
 ↓
Approved SRS v1.0
 ↓
RAG attribution
```

### Governance

Confirm:

* requirement ID is valid
* requirement is Approved
* source attribution is preserved
* no Draft requirement is accepted
* no invented requirement ID is accepted
* no orphan testcase is accepted

### Coverage

Confirm the live testcase contributes correctly to requirement coverage.

### Duplicate handling

Confirm the existing duplicate governance is applied normally.

Do not weaken or bypass any validator.

---

# 10. EVIDENCE REQUIREMENT

The report must distinguish clearly between what was:

### Previously verified

For example:

* CP-MVP2-03 implementation tests
* deterministic governance tests
* regression 47/47
* stub-based functional tests

and what is newly verified by this retry:

* real LLM invocation
* real LLM response
* real response parsing
* deterministic validation against real LLM output

Do not claim live LLM verification based on stub output.

---

# 11. POST-SMOKE REGRESSION

After the smoke test, run the full existing regression suite again.

Record the exact result.

Expected:

```text
47 passed, 0 failed
```

If the count has legitimately changed because of pre-existing repository state, report the actual result.

Do not alter tests merely to restore the expected count.

---

# 12. GIT / FILE INTEGRITY

After the smoke:

Check:

* Git status
* changed files
* diff
* current HEAD
* `origin/main`

The only expected repository changes from this task should be the smoke verification evidence/report artifacts.

If implementation files have changed:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not commit implementation changes.

---

# 13. REPORTS

Create:

### Detailed execution report

Under:

```text
docs/claude-execution-reports/CP-MVP2-03-LIVE-LLM-SMOKE/
```

Use a new timestamped filename.

The report must contain:

* execution timestamp
* environment
* starting Git state
* baseline regression
* environment/credential availability confirmation
* requirement used
* RAG retrieval evidence
* provider/model
* real LLM invocation result
* parsing result
* deterministic validation result
* traceability result
* coverage result
* duplicate/governance result
* post-smoke regression
* Git/file integrity
* limitations
* final result

Never include secrets.

### Summary report

Create:

```text
docs/claude-execution-reports/CP-MVP2-03-LIVE-LLM-SMOKE/summaries/
```

with a corresponding concise summary.

The summary must explicitly state:

```text
LIVE LLM INVOCATION: PASS / FAIL / BLOCKED
REAL RESPONSE PARSING: PASS / FAIL / BLOCKED
DETERMINISTIC VALIDATION: PASS / FAIL / BLOCKED
TRACEABILITY: PASS / FAIL / BLOCKED
REGRESSION: PASS / FAIL
OVERALL SMOKE RESULT: PASS / FAIL / BLOCKED
```

---

# 14. GATE INTERPRETATION

### PASS

Only if:

* real LLM call actually occurred
* real response was received
* response was parsed
* actual response passed through existing deterministic governance
* traceability was demonstrated
* Approved requirement was used through real RAG
* no specification or implementation changes were required
* regression remains green

### BLOCKED

If:

* credential is unavailable
* network/provider access is unavailable
* real LLM cannot be invoked
* another environment dependency prevents verification

A BLOCKED result due to environment is NOT an implementation failure.

### FAIL

If the real LLM path is successfully exercised but an implementation/governance defect is demonstrated.

Do NOT fix the defect during this task.

---

# 15. HARD STOP

If any implementation change appears necessary:

```text
STOP
REPORT
DO NOT MODIFY
DO NOT COMMIT
WAIT FOR HUMAN + DI DECISION
```

If the real smoke succeeds, do NOT continue into CP-MVP2-04.

This task ends with CP-MVP2-03 live smoke verification.

---

# 16. FINAL REPORTING

At the end, provide:

1. concise execution summary
2. exact smoke result
3. real LLM invocation status
4. parser status
5. deterministic validation status
6. traceability status
7. regression result
8. Git/file integrity result
9. report paths
10. commit hash if evidence was committed

Do not make any downstream changes.

**CP-MVP2-03 remains governed by the frozen v1.0 specification.**

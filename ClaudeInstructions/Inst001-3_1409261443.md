# TASK-CP-MVP2-03-PREP

## CP-MVP2-03 — LLM Testcase Generation

### PREPARATION / GOVERNANCE / EVIDENCE SETUP ONLY

---

# 0. EXECUTION MODE

This task is strictly a:

> **PREPARATION / GOVERNANCE / EVIDENCE SETUP TASK**

It does **NOT** authorize implementation of CP-MVP2-03.

The purpose is to establish and verify the repository foundation required for a future, separately authorized implementation task.

The governing lifecycle is:

```text
SPECIFY
   ↓
REVIEW
   ↓
FREEZE
   ↓
IMPLEMENT
   ↓
VERIFY
   ↓
GATE
```

For this task, we are only performing:

```text
PREPARE
   ↓
VERIFY
   ↓
REPORT
   ↓
COMMIT
   ↓
PUSH
   ↓
GATE
```

**DO NOT IMPLEMENT CP-MVP2-03.**

---

# 1. GOVERNING PRINCIPLES

The following principles are mandatory.

## 1.1 Frozen Specification

The CP-MVP2-03 Specification v1.0 has already been reviewed and approved by:

* Human Owner
* Di

It is:

> **APPROVED AND FROZEN**

It is immutable during this task.

---

## 1.2 Golden Rule

> **DO NOT CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

For this task there must be no implementation.

If any conflict exists between repository state, specification, governance, or task instructions:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

Do not silently resolve the conflict.

---

# 2. LOCAL REPOSITORY

The existing local MVP2 repository is:

```text
D:\AI PROJECTS\agentic-qe-mvp2
```

Claude MUST work in this repository.

Do NOT:

* create a parallel repository;
* create another MVP2 project;
* create an alternate working directory;
* copy the project into another repository.

---

# 3. EXISTING CP-MVP2-03 ARTIFACTS

The following files are already known to exist under:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs
```

Claude MUST inspect them before making changes.

---

## 3.1 CP-MVP2-03 Specification

Exact path:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\CP-MVP2-03-SPECIFICATION-v1.0.md
```

This is the canonical CP-MVP2-03 Specification v1.0.

Expected status:

```text
CP-MVP2-03 Specification v1.0 — APPROVED AND FROZEN
```

Claude MUST inspect the complete file.

If it exists and corresponds to the approved specification:

> **PRESERVE IT. DO NOT REWRITE IT.**

Do NOT:

* redesign;
* optimize;
* improve;
* simplify;
* expand;
* reduce;
* reinterpret;
* change requirements;
* change scope;
* change version;
* change expected behavior.

If it is missing, corrupted, empty, or materially inconsistent with the approved specification:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

Do not silently replace it.

---

## 3.2 Existing CP-MVP2-03 Preparation / Governance / Evidence Setup

Exact path:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\CP-MVP2-03 Preparation, Governance & Evidence Setup.md
```

This is an existing project artifact.

Claude MUST inspect the complete file before creating or modifying any governance/evidence documentation.

Do NOT assume this artifact is missing.

Determine exactly what it contains regarding:

* preparation;
* governance;
* evidence;
* execution;
* reporting;
* frozen specification handling;
* Claude execution rules;
* Git evidence;
* STOP conditions;
* checkpoint governance.

Preserve this existing file unless an explicit modification is authorized by this task.

Do NOT create duplicate or conflicting governance content.

---

# 4. CLAUDE EXECUTION & EVIDENCE GOVERNANCE

The expected governance artifact is:

```text
CLAUDE-EXECUTION-EVIDENCE-GOVERNANCE-v1.0.md
```

Before creating or modifying this file, inspect:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\CP-MVP2-03 Preparation, Governance & Evidence Setup.md
```

Determine whether the approved execution/evidence governance content:

1. already exists in the preparation file;
2. is referenced by it;
3. exists elsewhere in the repository;
4. genuinely requires a separate canonical document.

Do NOT duplicate governance documents unnecessarily.

Do NOT invent governing principles.

If the required governance content is missing and cannot be reconstructed from approved repository material without inventing new principles:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

---

# 5. REQUIRED INITIAL INSPECTION

Before making ANY changes, perform a complete inspection.

## 5.1 Repository State

Confirm:

* repository path;
* repository identity;
* current branch;
* current HEAD;
* working-tree status;
* configured remote(s).

Record the starting state.

---

## 5.2 Documentation Structure

Inspect:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs
```

Identify:

* specification directories/files;
* governance files;
* checkpoint documentation;
* Claude execution reports;
* summaries;
* existing documentation conventions.

---

## 5.3 CP-MVP2-01

Identify the frozen CP-MVP2-01 artifacts.

Record sufficient evidence to establish their state before this task.

---

## 5.4 CP-MVP2-02

Identify the frozen CP-MVP2-02 artifacts.

Record sufficient evidence to establish their state before this task.

---

## 5.5 SRS

Identify:

* Approved SRS;
* Draft SRS.

Record their relevant Git state.

---

## 5.6 Existing Execution Reports

Identify any existing Claude execution-report conventions.

Reuse existing repository conventions where appropriate.

Do not introduce unnecessary competing structures.

---

# 6. CP-MVP2-03 SPECIFICATION RECOVERY / CANONICALIZATION

The approved CP-MVP2-03 Specification v1.0 is already defined and frozen.

The existing file is:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\CP-MVP2-03-SPECIFICATION-v1.0.md
```

This task is a:

> **RECOVERY / PERSISTENCE / VERIFICATION ACTIVITY**

It is NOT a specification-design activity.

If the existing file is correct:

> Preserve it unchanged.

If it requires canonical placement elsewhere, do NOT move, rename, duplicate, or replace it automatically.

The preferred alternative location, if genuinely required by existing repository conventions, would be:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\specifications\CP-MVP2-03-SPEC-v1.0-FROZEN.md
```

However:

> **DO NOT create a duplicate canonical specification merely because this path is preferred.**

The existing file should remain the canonical artifact if it is already valid.

---

# 7. SPECIFICATION INTEGRITY RULES

Claude MUST NOT:

* add functional requirements;
* remove functional requirements;
* change requirement meaning;
* invent journeys;
* invent business rules;
* change acceptance criteria;
* change scenario expectations;
* alter scope;
* alter boundaries;
* change version 1.0;
* change frozen status;
* change the specification to accommodate implementation.

If a conflict is discovered:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

---

# 8. GOVERNANCE ARTIFACT HANDLING

Inspect the existing:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\CP-MVP2-03 Preparation, Governance & Evidence Setup.md
```

If it already contains the approved execution/evidence governance:

> Reuse it and do not create duplicate governance content unless a separate canonical artifact is explicitly necessary.

If a separate canonical governance document is necessary and its content is already approved/available, use:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\governance\CLAUDE-EXECUTION-EVIDENCE-GOVERNANCE-v1.0.md
```

Do not invent new governing principles.

---

# 9. EXECUTION REPORT STRUCTURE

Prepare the future execution-report structure.

Preferred:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\claude-execution-reports\CP-MVP2-03\
```

and:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\claude-execution-reports\CP-MVP2-03\summaries\
```

Expected structure:

```text
docs/
  claude-execution-reports/
    CP-MVP2-03/
      summaries/
```

Do NOT create a fake implementation report.

Only the actual preparation execution report and review summary described below may be created.

If Git does not preserve empty directories, use `.gitkeep` or another existing repository convention, provided this does not conflict with existing practices.

---

# 10. STRICT SCOPE BOUNDARY

This task permits ONLY:

1. Repository inspection.
2. Verification of the existing CP-MVP2-03 specification.
3. Canonicalization/persistence only if necessary and explicitly permitted.
4. Inspection/preservation of the existing preparation/governance artifact.
5. Creation of the governance artifact only if genuinely required and supported by approved content.
6. Creation of execution-report directory structure.
7. Integrity validation.
8. Detailed execution report.
9. Di Review Summary.
10. Git commit.
11. Git push.
12. Verification of push.

---

# 11. ABSOLUTELY PROHIBITED

Do NOT perform:

* CP-MVP2-03 implementation;
* LLM testcase generation;
* LLM integration;
* testcase-generator implementation;
* testcase-generation prompt implementation;
* new RAG implementation;
* browser automation;
* Playwright CP-MVP2-03 implementation;
* API execution;
* test-data execution;
* performance execution;
* security execution;
* penetration testing;
* self-healing;
* CI/CD;
* CP-MVP2-04;
* downstream checkpoint implementation;
* Demo Web Shop requirements discovery;
* Demo Web Shop crawling/inspection for requirements;
* unrelated refactoring;
* framework replacement;
* creation of a parallel framework.

---

# 12. FROZEN ARTIFACT INTEGRITY

Before committing, verify using actual Git evidence:

* CP-MVP2-01 frozen artifacts unchanged.
* CP-MVP2-02 frozen artifacts unchanged.
* Approved SRS unchanged.
* Draft SRS unchanged.
* Existing implementation unchanged.
* No unrelated files changed by this task.

Record:

```text
Frozen artifacts modified: YES / NO
```

Expected:

```text
Frozen artifacts modified: NO
```

If any frozen artifact was changed by this task:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

Do NOT:

* revert it;
* overwrite it;
* reset it;
* conceal it;
* discard it;

without Human + Di direction.

---

# 13. PRE-EXISTING WORKING-TREE CHANGES

If the repository contains pre-existing uncommitted changes before this task:

1. Record them.
2. Do not assume they were created by this task.
3. Do not modify them.
4. Do not revert them.
5. Do not include them in the task commit unless they are explicitly authorized artifacts of this task.

The execution report MUST distinguish:

```text
Pre-existing changes
```

from:

```text
Changes created by this task
```

---

# 14. SCOPE AUDIT BEFORE COMMIT

Before committing, explicitly verify:

```text
CP-MVP2-03 implementation: NOT PERFORMED
LLM testcase generation: NOT IMPLEMENTED
LLM integration: NOT IMPLEMENTED
New RAG functionality: NOT IMPLEMENTED
CP-MVP2-04: NOT IMPLEMENTED
Downstream functionality: NOT IMPLEMENTED
Unrelated refactoring: NOT PERFORMED
```

---

# 15. REPORT 1 — DETAILED EXECUTION EVIDENCE REPORT

This task MUST produce a **very detailed permanent execution/audit report**.

Preferred location:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\claude-execution-reports\CP-MVP2-03\
```

Filename:

```text
CP-MVP2-03-EXEC-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

Where:

* `YYYYMMDD` = execution date;
* `HHMMSS` = execution time;
* `<UNIQUE-ID>` = unique identifier.

The filename MUST be unique.

Do NOT overwrite an earlier report.

The report MUST include:

## Execution Identity

* Execution ID.
* Checkpoint.
* Task.
* Date/time.
* Repository.
* Local repository path.
* Branch.
* Starting commit.
* Ending commit.

## Instruction Received

Summarize the complete task received, including scope restrictions and STOP conditions.

## Initial Repository State

Record:

* repository;
* branch;
* starting HEAD;
* working-tree status;
* remote;
* relevant existing artifacts.

## Existing CP-MVP2-03 Specification

Record:

* exact path;
* existence;
* version;
* status;
* validation result;
* whether modified.

## Existing Preparation/Governance Artifact

Record:

* exact path;
* inspection result;
* relevant content;
* whether preserved;
* whether modified.

## Actions Performed

List exactly what was done.

## Files Created

List every newly created file.

## Files Modified

List every modified file.

## Files Deleted

If none:

```text
None
```

Otherwise list every deleted file.

## Pre-existing Changes

Explicitly distinguish pre-existing changes from changes caused by this task.

## CP-MVP2-01 Integrity

Provide actual evidence.

## CP-MVP2-02 Integrity

Provide actual evidence.

## SRS Integrity

Provide actual evidence for:

* Approved SRS;
* Draft SRS.

## Scope Audit

Explicitly confirm no implementation occurred.

## Validation

Record actual commands/checks and actual results.

Do NOT fabricate results.

## Git Evidence

Include:

* starting status;
* final status;
* diff/stat;
* changed files;
* commit SHA;
* branch;
* remote;
* push result.

## Deviations

Mandatory.

If none:

```text
Deviations from instruction/specification: NONE
```

Otherwise list every deviation.

## Problems Encountered

Mandatory.

If none:

```text
Problems encountered: NONE
```

Otherwise list every actual problem.

## Human + Di Decision Required

If none:

```text
Human + Di decision required: NONE
```

Otherwise state exactly what decision is required.

## Final Result

Use exactly one:

```text
PASS
FAIL
BLOCKED
PARTIAL
```

The result MUST be supported by actual evidence.

---

# 16. REPORT 2 — DI REVIEW SUMMARY

Create a second, separate, concise report specifically intended for:

> **Human + Di review and checkpoint gating**

This is NOT a replacement for the detailed report.

Preferred location:

```text
D:\AI PROJECTS\agentic-qe-mvp2\docs\claude-execution-reports\CP-MVP2-03\summaries\
```

Filename:

```text
CP-MVP2-03-SUMMARY-YYYYMMDD-HHMMSS-<UNIQUE-ID>.md
```

The filename MUST be unique.

The summary MUST contain:

```text
Execution ID:

Checkpoint:
CP-MVP2-03

Task:
TASK-CP-MVP2-03-PREP

Result:

Specification:
CP-MVP2-03 Specification v1.0 — APPROVED AND FROZEN

Specification path:

Existing preparation/governance artifact:

Repository:

Local repository path:

Branch:

Starting commit:

Ending commit:

Actions completed:

<concise evidence-based summary>

CP-MVP2-01 frozen artifacts:
UNCHANGED / CHANGED

CP-MVP2-02 frozen artifacts:
UNCHANGED / CHANGED

Approved SRS:
UNCHANGED / CHANGED

Draft SRS:
UNCHANGED / CHANGED

CP-MVP2-03 implementation:
NOT IMPLEMENTED

LLM testcase generation:
NOT IMPLEMENTED

New RAG functionality:
NOT IMPLEMENTED

Deviations:
NONE / <details>

Problems:
NONE / <details>

Checks:
<actual results>

Commit:
<commit SHA>

Push:
SUCCESS / FAILURE

Detailed execution report:
<local path and GitHub path/link>

Human + Di decision required:
YES / NO
```

This summary is intended to allow Human + Di to quickly determine whether the preparation checkpoint is safe to gate.

---

# 17. BOTH REPORTS ARE MANDATORY REPOSITORY ARTIFACTS

Both reports MUST:

1. Be created locally.
2. Be verified.
3. Be included in the Git commit.
4. Be pushed to GitHub.
5. Be referenced in the final Claude response.

Do NOT report the task as fully complete if either report remains uncommitted or unpushed.

---

# 18. REQUIRED EXECUTION ORDER

The complete sequence MUST be:

```text
INSPECT
   ↓
VERIFY EXISTING SPECIFICATION
   ↓
VERIFY EXISTING GOVERNANCE/PREPARATION ARTIFACT
   ↓
PREPARE ONLY AUTHORIZED MISSING ARTIFACTS
   ↓
FROZEN ARTIFACT INTEGRITY CHECK
   ↓
SCOPE AUDIT
   ↓
CREATE DETAILED EXECUTION REPORT
   ↓
CREATE DI REVIEW SUMMARY
   ↓
VERIFY BOTH REPORTS
   ↓
GIT STATUS / DIFF / STAT
   ↓
COMMIT
   ↓
PUSH
   ↓
VERIFY REMOTE PUSH
   ↓
FINAL RESPONSE
```

---

# 19. GIT COMMIT

Only after all checks pass:

Recommended commit message:

```text
docs: establish CP-MVP2-03 specification and execution governance
```

Before commit record:

* starting HEAD;
* branch;
* working-tree status;
* changed files;
* diff/stat;
* frozen-artifact integrity;
* scope audit.

After commit record:

* commit SHA;
* branch;
* final status.

Do NOT include unrelated pre-existing changes.

---

# 20. GIT PUSH

Push the authorized commit to the appropriate remote branch.

Verify actual success.

Record:

* repository;
* branch;
* commit SHA;
* push result.

Do NOT claim successful push unless Git confirms it.

If push fails:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

---

# 21. STOP CONDITIONS

Immediately stop and report if:

* frozen CP-MVP2-03 specification must be changed;
* approved specification is unclear;
* existing specification conflicts with approved specification;
* existing preparation/governance artifact conflicts with approved governance;
* CP-MVP2-01 must be modified;
* CP-MVP2-02 must be modified;
* Approved SRS must be modified;
* Draft SRS must be modified;
* unexpected Git changes cannot safely be distinguished;
* implementation appears necessary;
* governance principles would have to be invented;
* push cannot be completed;
* any requested action violates the frozen scope.

Use:

```text
STOP → REPORT → HUMAN + DI DECIDE
```

Do NOT solve a STOP condition by changing the specification.

---

# 22. FINAL RESPONSE TO HUMAN + DI

Your final response MUST be concise but evidence-based.

Provide:

1. Execution ID.
2. Result.
3. Repository.
4. Local repository path.
5. Specification path.
6. Existing preparation/governance artifact path.
7. What was inspected.
8. What was created.
9. What was preserved.
10. CP-MVP2-01 integrity result.
11. CP-MVP2-02 integrity result.
12. Approved SRS integrity result.
13. Draft SRS integrity result.
14. Confirmation CP-MVP2-03 implementation was NOT performed.
15. Confirmation LLM testcase generation was NOT implemented.
16. Confirmation new RAG functionality was NOT implemented.
17. Deviations.
18. Problems encountered.
19. Commit SHA.
20. Branch.
21. Push status.
22. Detailed execution report path/link.
23. Di Review Summary path/link.
24. Human + Di decision required: YES / NO.

Do not merely say:

```text
Done.
```

---

# 23. FINAL BOUNDARY

This task establishes and verifies the governance foundation for:

**CP-MVP2-03 — LLM Testcase Generation**

It does NOT authorize CP-MVP2-03 implementation.

After this PREP task is independently reviewed by Human + Di, a **separate implementation task** will authorize:

**CP-MVP2-03 — LLM Testcase Generation Implementation**

That future task must use:

* CP-MVP2-03 Specification v1.0 — FROZEN;
* approved execution/evidence governance;
* existing MVP2 architecture;
* frozen CP-MVP2-01;
* frozen CP-MVP2-02.

---

# 24. GOLDEN RULE

> **DO NOT CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

For this task there should be no implementation to make pass.

The objective is to establish the authoritative specification, governance, preparation, and evidence foundation cleanly and verifiably.

**SPECIFY → REVIEW → FREEZE → IMPLEMENT → VERIFY → GATE**

Current phase:

**PREPARE → VERIFY → REPORT → COMMIT → PUSH → GATE**

**NO IMPLEMENTATION YET.**

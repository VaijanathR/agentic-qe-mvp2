# CP-MVP2-06 — SPECIFICATION CREATION + SELF-REVIEW + FREEZE

## Single Batched Governance Instruction

You are operating inside the MVP2 repository:

`agentic-qe-mvp2`

We have completed and frozen CP-MVP2-01 through CP-MVP2-05.

The repository's frozen state must be treated as the baseline.

---

# 1. GOVERNANCE — NON-NEGOTIABLE

Follow:

**SPECIFY → REVIEW → FREEZE → IMPLEMENT → VERIFY → GATE**

For this execution, we are doing ONLY:

**SPECIFY → SELF-REVIEW → FREEZE**

Do NOT implement CP-MVP2-06.

Do NOT create browser execution code.

Do NOT create Playwright runners.

Do NOT execute the browser.

Do NOT begin CP-MVP2-07, CP-MVP2-08 or CP-MVP2-09.

Do NOT modify any CP-MVP2-01 through CP-MVP2-05 frozen artifact.

Do NOT modify the Approved SRS.

Do NOT change existing requirements, testcases, test data, or CP-05 automation artifacts.

Do NOT "improve" earlier checkpoints.

The frozen artifacts are contracts.

### Golden rule

**NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

If you discover a conflict between an existing frozen artifact and a proposed CP-06 design:

**STOP → DOCUMENT THE CONFLICT → DO NOT MODIFY THE FROZEN ARTIFACT → REPORT IT.**

No silent reinterpretation is permitted.

---

# 2. OBJECTIVE

Create the canonical CP-MVP2-06 specification for:

## REAL BROWSER EXECUTION

CP-06 is the execution boundary between the governed Playwright automation artifacts produced by CP-05 and actual browser execution against the Demo Web Shop SUT.

The intended lifecycle is:

**Approved SRS**
→ **MVP2 Knowledge Base / governed RAG**
→ **Accepted CP-03 Testcase**
→ **Accepted/Governed CP-04 Test Data**
→ **Accepted/Governed CP-05 Playwright Artifact**
→ **CP-06 Real Browser Execution**
→ **Execution Evidence**
→ **Execution Result**
→ **Failure Classification / Governance**
→ **CP-06 Result**

CP-06 must execute governed artifacts; it must NOT regenerate or reinterpret the business intent.

---

# 3. FROZEN UPSTREAM CONTRACTS

Before writing the specification, inspect the actual repository and verify the current frozen artifacts for:

* CP-MVP2-01
* CP-MVP2-02
* CP-MVP2-03
* CP-MVP2-04
* CP-MVP2-05
* Approved SRS

Use the repository itself as the source of truth.

Do not assume filenames or commit IDs where repository inspection can establish them.

Confirm that CP-05 is frozen and use its actual schema and governance rules as the immediate upstream contract.

The CP-05 artifact is the input to CP-06.

CP-06 must not redefine:

* requirement meaning
* testcase meaning
* testcase coverage
* test data meaning
* locator evidence
* Playwright artifact acceptance rules
* CP-05 governance decisions

---

# 4. CP-06 SPECIFICATION MUST DEFINE

Create:

`docs/CP-MVP2-06-SPECIFICATION-v1.0.md`

The specification must be detailed enough that a separate implementation agent can implement CP-06 without guessing.

At minimum define the following.

---

## 4.1 Objective

Define exactly what CP-06 owns and what it does not own.

CP-06 owns real browser execution of accepted/governed CP-05 Playwright artifacts and production of trustworthy execution evidence and results.

It does not own:

* requirement interpretation
* testcase generation
* test-data generation
* Playwright artifact generation
* locator invention
* self-healing
* requirement modification
* test-data modification
* browser-grid orchestration unless explicitly justified and included in MVP2 scope

---

# 5. EXECUTION ELIGIBILITY

Define deterministic rules for which CP-05 artifacts may be executed.

At minimum address:

* ACCEPTED
* POSSIBLE_DUPLICATE
* REJECTED
* QUARANTINED
* UNSUPPORTED_NEEDS_CLARIFICATION

Execution eligibility must be explicit.

Do not invent an execution rule that conflicts with CP-05.

The implementation must never silently execute an artifact that CP-05 has declared unsupported.

---

# 6. BROWSER SCOPE

Define the MVP2 browser execution scope.

The CP-05 frozen specification currently establishes:

`browser_intent = chromium-headless`

Respect that existing contract.

Do not silently expand MVP2 to:

* Firefox
* WebKit
* browser grids
* multiple VMs
* cloud browser farms
* parallel browser orchestration

unless the specification explicitly identifies such functionality as future/post-MVP scope.

If browser expansion is desirable, document it as future scope rather than implementing/designing it implicitly.

---

# 7. EXECUTION INPUT CONTRACT

Define the exact logical inputs required by CP-06.

At minimum consider:

* accepted CP-05 Playwright artifact
* testcase ID
* requirement IDs
* test-data-set ID
* journey ID
* scenario type
* browser intent
* preconditions
* Playwright steps
* locator specifications
* assertions
* evidence requirements
* source attribution
* generation metadata
* CP-05 validation status

The specification must make clear which inputs are authoritative and which are metadata.

---

# 8. EXECUTION ENGINE

Specify the conceptual execution engine.

Define how CP-06:

1. Loads the governed CP-05 artifact.
2. Validates execution eligibility.
3. Resolves the browser intent.
4. Establishes the browser/context/page.
5. Applies preconditions.
6. Executes each governed step.
7. Evaluates deterministic assertions.
8. Captures required evidence.
9. Records execution status.
10. Classifies failures.
11. Produces the final execution result.

Do not implement these functions during this task.

This is a specification only.

---

# 9. EXECUTION RESULT SCHEMA

Define a structured result model.

At minimum consider:

* execution_id
* automation_id
* testcase_id
* requirement_ids
* test_data_set_id
* journey_id
* scenario_type
* browser
* execution_start
* execution_end
* duration
* overall_status
* step_results
* assertion_results
* evidence_references
* failure_classification
* failure_summary
* source_attribution
* environment_metadata
* governance_status
* reproducibility metadata
* notes

Clearly distinguish:

**PASS / FAIL / BLOCKED / NOT_EXECUTED / ERROR**

Do not introduce statuses without defining their semantics.

---

# 10. STEP RESULT MODEL

Define what evidence is captured for each executable step.

At minimum consider:

* step number
* action
* target/locator reference
* status
* start/end time
* duration
* observed result
* expected condition where applicable
* evidence references
* error information

Avoid storing unnecessary secrets or sensitive information.

---

# 11. ASSERTION EXECUTION

Respect the CP-05 assertion model.

CP-05 distinguishes:

* BUSINESS_REQUIRED
* TECHNICAL_USEFUL
* DIAGNOSTIC_OPTIONAL

The CP-06 specification must explicitly state how these are executed and how they influence the final result.

Do not allow a technical or diagnostic assertion to silently override a business assertion.

Define precedence where necessary.

---

# 12. EVIDENCE MODEL

Evidence is a critical part of CP-06.

Define what execution evidence should be produced.

At minimum consider:

* execution logs
* step-level logs
* assertion results
* screenshots on failure
* screenshots where explicitly required
* browser/Playwright diagnostics
* final execution summary
* artifact/result linkage

Every reported execution conclusion must be traceable to evidence.

Do not fabricate evidence.

Do not claim browser execution occurred unless it actually occurred.

---

# 13. FAILURE CLASSIFICATION

Define deterministic failure categories.

At minimum consider the existing Agentic QE failure taxonomy:

1. Requirement mismatch / ambiguity
2. Test-data issue
3. Authorization issue
4. Tool issue
5. Environment issue
6. Third-party dependency issue
7. Constraint violation

Also consider whether additional execution-specific categories are required, such as:

* locator failure
* assertion failure
* timeout
* browser launch failure
* navigation failure

If these are introduced, define how they relate to the existing taxonomy.

Do not hide one category inside another merely for convenience.

---

# 14. FAILURE GOVERNANCE

Define what CP-06 must do when execution fails.

The agent must NOT:

* change the requirement
* change the testcase
* change the test data
* change the expected response
* invent a locator
* weaken an assertion
* remove a failing step
* retry indefinitely
* classify a failure as PASS without evidence

If a simple deterministic retry is allowed, define exactly when and why.

If retry behavior is not justified by the available evidence, explicitly prohibit it.

---

# 15. SELF-HEALING BOUNDARY

CP-05 explicitly does not include self-healing.

Therefore CP-06 must clearly define whether self-healing is:

* prohibited in CP-06
* limited to non-semantic runtime recovery
* deferred to a later checkpoint

Do not allow CP-06 to silently mutate the frozen CP-05 automation artifact.

Any proposed locator repair or artifact mutation must be treated as a governance-controlled future capability unless explicitly specified.

---

# 16. ENVIRONMENT HANDLING

Define environment assumptions.

At minimum document:

* SUT URL
* browser availability
* Playwright runtime dependency
* network dependency
* authentication/precondition assumptions
* environment configuration
* timeout policy
* artifact/evidence output location

Do not invent credentials.

Do not embed secrets.

Do not assume credentials exist unless the repository/environment explicitly establishes them.

If a required precondition cannot be established, execution should be governed as NOT_EXECUTED/BLOCKED according to the defined semantics rather than falsely reported as FAIL or PASS.

---

# 17. DETERMINISTIC GOVERNANCE VALIDATION

CP-06 must have deterministic checks before execution.

Define validation for:

* schema integrity
* execution eligibility
* required fields
* locator validity
* action compatibility
* browser intent
* testcase/requirement traceability
* test-data linkage
* assertion presence
* evidence requirements
* unsupported conditions

The LLM must not be required to decide whether an artifact is executable.

Where possible, governance decisions must be deterministic.

---

# 18. RUNTIME VS DESIGN-TIME FAILURES

Clearly distinguish:

### Design-time failure

The CP-05 artifact is invalid/unsupported before browser execution.

### Runtime failure

The artifact was eligible and execution started, but execution encountered a failure.

This distinction must be visible in reporting.

Do not report an unsupported CP-05 artifact as a browser execution failure.

---

# 19. TRACEABILITY

Define the complete execution trace:

`Execution ID`
→ `Automation ID`
→ `Testcase ID`
→ `Requirement ID(s)`
→ `Test Data Set ID`
→ `Approved SRS`
→ `source attribution`

Execution evidence must be traceable back to the governed artifact.

No orphan execution results.

---

# 20. REPRODUCIBILITY

Define what metadata is required to make an execution reproducible.

At minimum consider:

* execution ID
* timestamp
* browser/version where available
* Playwright version where available
* Python/runtime version where applicable
* SUT URL
* artifact version/commit
* test-data reference
* environment metadata
* execution configuration

Do not claim audit-grade reproducibility unless the implementation can actually provide it.

---

# 21. SECURITY / DATA HANDLING

Define evidence/data handling rules.

The system must not expose:

* passwords
* tokens
* API keys
* secrets
* real payment credentials
* unnecessary personal information

Screenshots/logs must be sanitized where required.

Use synthetic test data from CP-04.

Do not invent production credentials.

---

# 22. REPORTING

Define the CP-06 execution report.

The report should provide:

* execution summary
* total eligible artifacts
* executed
* passed
* failed
* blocked
* not executed
* error
* coverage
* requirement traceability
* evidence locations
* failure classification
* governance findings
* limitations
* next steps

Clearly distinguish:

**execution coverage**

from

**requirements/testcase coverage**

Do not claim that browser execution automatically increases requirement coverage.

---

# 23. COVERAGE

Define CP-06 execution coverage carefully.

At minimum distinguish:

### Eligible artifact execution coverage

Number of eligible CP-05 artifacts actually attempted / total eligible CP-05 artifacts.

### Successful execution

Number of eligible artifacts that completed with PASS.

### Requirement coverage

Inherited from upstream CP-03/CP-05 governance and must not be redefined casually.

Do not allow an execution PASS to imply requirements were adequately covered.

---

# 24. LIVE EXECUTION EVIDENCE

Define what constitutes valid evidence that a real browser execution occurred.

For example:

* actual Playwright/browser process
* actual SUT interaction
* execution timestamps
* browser logs
* screenshots/evidence
* deterministic result records

Do not accept:

* mocked browser execution
* fabricated screenshots
* synthetic PASS records
* stubbed execution presented as real execution

If mocks/stubs are useful for unit tests, explicitly distinguish them from live execution evidence.

---

# 25. TESTING OF CP-06 ITSELF

Define implementation verification requirements.

At minimum require:

### Unit tests

For deterministic components.

### Integration tests

For execution pipeline behavior.

### Negative tests

For invalid/unsupported artifacts.

### Governance tests

For eligibility and forbidden behavior.

### Live browser verification

At least one real governed CP-05 artifact should be used if prerequisites permit.

Clearly distinguish:

* deterministic/unit evidence
* mocked/stubbed evidence
* real browser evidence

Do not combine them into one misleading result.

---

# 26. LIVE BROWSER TEST POLICY

The first live browser verification must use an existing governed CP-05 artifact.

Do not create a new business scenario merely to make the browser test pass.

If no currently ACCEPTED CP-05 artifact is executable because of the known CP-05 evidence gaps, document that fact.

Do not alter CP-05 to manufacture an executable artifact.

A governed NOT_EXECUTED/BLOCKED result is preferable to fabricated success.

---

# 27. CP-06 OUTPUT ARTIFACTS

Define the expected CP-06 implementation outputs, but do not create implementation files during this specification task.

Likely categories include:

* execution schema
* deterministic execution validator
* browser execution engine
* evidence manager
* result generator
* failure classifier
* tests
* execution reports
* summaries
* final checkpoint

Use exact filenames only where justified.

Do not create placeholders.

---

# 28. DOWNSTREAM CONTRACT

Define what CP-07 may consume from CP-06.

CP-06 should provide sufficient structured evidence for:

* failure analysis
* RCA
* replanning
* governance
* human review

But CP-06 must not implement CP-07.

Clearly mark CP-07 as downstream/not started.

Similarly:

* CP-08 performance remains separate.
* CP-09 unified reporting remains downstream.

---

# 29. KNOWN LIMITATIONS

Inspect the existing repository and carry forward relevant known limitations from CP-01 through CP-05.

At minimum consider:

* CP-05 locator gaps
* REQ-REG-01 locator evidence gap
* payment locator uncertainty
* cart selector uncertainty
* DATA-OQ-01
* browser environment prerequisites
* authentication/precondition limitations
* DOM evidence boundary
* action-type evidence advisory
* any other actual repository limitation

Do not invent resolutions.

Do not silently remove existing advisories.

---

# 30. ACCEPTANCE CRITERIA

Create explicit CP-06 acceptance criteria.

The specification should not be considered implementation-ready unless:

* scope is unambiguous
* upstream contracts are preserved
* executable eligibility is deterministic
* browser scope is explicit
* execution result schema is defined
* evidence requirements are defined
* failure taxonomy is defined
* governance behavior is defined
* traceability is defined
* reproducibility expectations are honest
* live execution evidence requirements are explicit
* security/data-handling rules are explicit
* downstream boundaries are explicit
* known limitations are recorded
* no hidden CP-07/08/09 functionality is introduced

---

# 31. GOVERNANCE STATUS

Initially create the specification as:

`DRAFT — pending Human + Di review`

However, this instruction uses a batched process.

After completing the draft, perform a rigorous self-review against:

* frozen CP-01 through CP-05
* Approved SRS
* MVP2 README
* CP-05 specification
* CP-05 implementation
* existing governance principles
* this instruction

If the self-review finds NO BLOCKING ISSUE:

1. update the specification status to APPROVED/FROZEN
2. create the formal specification-freeze checkpoint
3. calculate and record the specification SHA-256
4. run the existing regression suite
5. confirm no upstream frozen artifact changed
6. commit and push the specification freeze

If the self-review finds ANY BLOCKING ISSUE:

* DO NOT freeze.
* Leave the specification in DRAFT.
* Document the blocker clearly.
* Commit only if appropriate and safe.
* Report exactly what requires Human + Di decision.

Do not manufacture a PASS.

---

# 32. SPECIFICATION FREEZE CHECKPOINT

If and only if the specification passes self-review, create:

`docs/CP-MVP2-06-FINAL-SPECIFICATION-FREEZE-CHECKPOINT.md`

It must record:

* CP-06 status
* specification path
* specification commit
* specification SHA-256
* upstream frozen artifacts verified unchanged
* regression result
* CP-06 implementation status = NOT STARTED
* CP-07/08/09 status = NOT STARTED
* known advisories/limitations
* governance statement
* final status

Required final status if successful:

**CP-MVP2-06 SPECIFICATION FINAL GATE: PASS — SPECIFICATION FROZEN / CHECKPOINT CLOSED**

---

# 33. EXECUTION REPORT

Create a detailed Claude execution report under:

`docs/claude-execution-reports/CP-MVP2-06/`

Use the repository's established naming convention.

The report must document:

* objective
* repository state inspected
* frozen upstream contracts inspected
* specification decisions
* governance decisions
* self-review
* tests run
* SHA verification
* files created
* files intentionally NOT created
* blockers/advisories
* final status

---

# 34. SUMMARY REPORT

Create a concise summary under:

`docs/claude-execution-reports/CP-MVP2-06/summaries/`

Follow the established naming convention used by CP-01 through CP-05.

---

# 35. ABSOLUTE IMPLEMENTATION BOUNDARY

Before finishing, verify that this execution did NOT create:

* browser runner
* browser execution code
* Playwright execution implementation
* CP-06 runtime engine
* execution evidence generated from a real browser
* CP-07 implementation
* CP-08 implementation
* CP-09 implementation

The only intended CP-06 artifacts for this execution are:

1. specification
2. specification-freeze checkpoint, IF PASS
3. detailed execution report
4. summary report

---

# 36. REGRESSION

Run the full existing regression suite after specification/checkpoint changes.

The regression must pass.

If regression fails:

**DO NOT modify frozen upstream implementation to make it pass.**

Investigate and report.

---

# 37. GIT DISCIPLINE

Use normal Git workflow.

Before starting:

* inspect current branch
* inspect working tree
* inspect current HEAD
* establish baseline

After completion:

* review changed files
* ensure no unintended files changed
* ensure no frozen artifact changed
* commit the intended CP-06 specification artifacts
* push to origin/main
* confirm:

`HEAD == origin/main`

and the working tree is clean.

Do not rewrite history.

Do not force-push.

---

# 38. REQUIRED FINAL RESPONSE

Your final response to me must be concise but complete.

Include:

1. Final status
2. Final commit SHA
3. Specification path + clickable GitHub URL
4. Specification-freeze checkpoint + clickable GitHub URL
5. Detailed execution report + clickable GitHub URL
6. Summary + clickable GitHub URL
7. Specification SHA-256
8. Regression result
9. Upstream frozen-artifact verification
10. Any advisories/limitations
11. Explicit confirmation:

**CP-06 IMPLEMENTATION: NOT STARTED**

12. Explicit confirmation:

**CP-07/08/09: NOT STARTED**

If successful, state exactly:

**CP-MVP2-06 SPECIFICATION FINAL GATE: PASS — SPECIFICATION FROZEN / CHECKPOINT CLOSED**

Do not provide implementation code.

Do not begin CP-06 implementation.

---

# 39. FINAL GOVERNANCE PRINCIPLE

This is a specification/freeze cycle only.

We are deliberately separating:

**CP-06 specification**

from

**CP-06 implementation**

and preserving the frozen contracts from CP-01 through CP-05.

The purpose of this batched instruction is to reduce unnecessary interaction loops, NOT to reduce governance quality.

Execute the complete batched cycle now.

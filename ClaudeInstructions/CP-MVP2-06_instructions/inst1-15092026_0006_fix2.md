# MVP2 — CR-001 Governance Reconciliation

## Governance-Only Finalization After Batch 1

You are now performing a **governance reconciliation and repository-consistency task only** following completion of CR-001 Batch 1.

This is **NOT a new implementation cycle**.

The Batch 1 implementation is already complete and must NOT be re-engineered, expanded, or silently changed.

---

## 1. GOVERNANCE RULE — NON-NEGOTIABLE

Follow:

**SPECIFY → REVIEW → FREEZE → IMPLEMENT → VERIFY → GATE**

Di has independently reviewed the Batch 1 implementation.

Current engineering assessment:

* Batch 1 implementation: **PASS with advisories**
* Governance state: **BLOCKED only because the canonical CR-001 governance documents are not internally reconciled with the implementation/approval state**

Your task is therefore limited to reconciling the governance documentation and producing evidence.

Do NOT declare the Di gate PASS or APPROVED.

Only Di performs the final independent gate.

---

# 2. DO NOT IMPLEMENT ANYTHING NEW

Do NOT:

* add new functional implementation
* redesign the persistence architecture
* modify the RBTP implementation
* modify testcase generation
* modify test-data generation
* modify Playwright generation
* modify CP-06 execution
* modify frozen CP-MVP2-01 through CP-MVP2-06 specifications
* modify the Approved SRS
* modify `llm/client.py`
* fix the CP-05 automation-ID collision
* perform security/penetration implementation
* reopen or rewrite completed checkpoints
* change test results
* regenerate evidence merely to obtain different results

The existing Batch 1 implementation remains the implementation under review.

---

# 3. RECONCILE CR-001 GOVERNANCE STATE

Inspect the actual repository and reconcile:

`docs/CP-MVP2-CR-001-PERSISTENT-LIFECYCLE-v1.0.md`

and:

`docs/CP-MVP2-CR-001-ARCHITECTURE-IMPACT-ASSESSMENT-v1.0.md`

with the actual Batch 1 implementation and evidence.

The current inconsistency is:

* implementation proceeded under the corrected CR-001 direction
* implementation report records the corrected RBTP definition and Batch 1 authorization
* but the canonical CR documents still contain stale PROPOSED / PENDING REVIEW language

Resolve this inconsistency.

The final CR must clearly establish the governance state for Batch 1.

---

# 4. RBTP DEFINITION — MUST BE CORRECT

The canonical definition must be:

**RBTP = Risk-Based Test Prioritization**

It must NOT be:

**Requirement-Based Test Plan**

Remove/reconcile any stale wording that implies the latter definition.

The CR should explicitly state that RBTP answers:

> **What should we test first, and why?**

It must remain distinct from the Dependency/Reusability Matrix:

> **What depends on what, what can be reused, and what should not be retested unnecessarily?**

---

# 5. RECORD THE APPROVED BATCH 1 ARCHITECTURAL DECISIONS

The reconciled CR must explicitly record the following Batch 1 decisions.

### 5.1 Persistent lifecycle artifacts

Batch 1 establishes persistence for:

* Testcases
* Requirement ↔ Testcase traceability
* RBTP
* Dependency/Reusability Matrix
* Test Data
* Testcase ↔ Test Data traceability
* Playwright Automation
* Automation ↔ Testcase traceability
* reusable-component candidates/evidence where applicable
* Execution persistence
* Execution Summary

The architecture remains:

**REQ ↔ TC ↔ RBTP ↔ DATA ↔ AUTO ↔ COMPONENT ↔ EXECUTION ↔ EVIDENCE ↔ RESULT ↔ REPORT**

### 5.2 Persistence model

Record that persistence is additive to the frozen checkpoint implementations.

Existing frozen behavior must remain intact.

No retroactive rewriting of frozen checkpoint artifacts is permitted.

### 5.3 Timestamped execution persistence

Record the Batch 1 decision that timestamped execution persistence is supported **additively/backward-compatibly**.

Do NOT break or rename existing frozen CP-06 execution evidence paths.

Where applicable, the preferred future hierarchy may be:

`runs/YYYY/MM/DD/<execution_id>/`

while preserving existing:

`runs/cp_mvp2_06/<execution_id>/`

compatibility.

Do not modify frozen CP-06 behavior merely to standardize the path.

---

# 6. RECORD THE RBTP GOVERNANCE RULES

The CR must accurately record:

* LLM may propose risk factors.
* Deterministic governance validates them.
* No fabricated risk information.
* Unknown evidence remains `UNKNOWN`.
* Risk rationale must be explainable.
* Risk priority must be deterministic.
* Evidence/source attribution must be preserved.
* Risk prioritization must not silently alter requirements or testcase meaning.

The current implementation's deterministic P0–P3 prioritization may be documented as the Batch 1 implementation behavior.

Do not invent additional risk factors or scoring rules.

---

# 7. RECORD THE TWO KNOWN ADVISORIES

## Advisory A — AUTOMATION_ID_COLLISION_ADVISORY

Record exactly that Batch 1 discovered a real CP-05 architectural limitation:

The frozen CP-05 automation ID does not incorporate the test-data-set identity, so multiple automation artifacts generated for the same testcase with different eligible datasets can share the same `automation_id`.

Record:

* no data was lost
* persistence remains version-safe
* this was not silently corrected
* this is a future CP-05 Change Request candidate
* it is NOT a Batch 1 failure

Do not modify CP-05 to fix it now.

---

## Advisory B — LIVE CLAUDE RBTP VERIFICATION

Record that live Claude RBTP verification was attempted but blocked by the execution environment's PATH resolution:

`shutil.which("claude") -> None`

Record:

* this was honestly reported
* no workaround was used to fabricate a live-Claude result
* deterministic tests passed
* stub-LLM narrative validation passed
* this remains an advisory/verification limitation
* it does NOT justify changing the implementation or governance rules

Do not falsely claim live Claude RBTP verification passed.

---

# 8. SECURITY / PENETRATION TESTING

Preserve the existing governance decision:

**Security / penetration testing remains POST-MVP / PARKED.**

The Batch 1 security document is design-only.

Do not implement security testing now.

Do not convert the design boundary into implementation scope.

---

# 9. PRESERVE FROZEN CHECKPOINTS

Verify that the following remain unchanged:

* CP-MVP2-01
* CP-MVP2-02
* CP-MVP2-03
* CP-MVP2-04
* CP-MVP2-05
* CP-MVP2-06
* Approved SRS
* `llm/client.py`

Do not modify them.

If you discover that any frozen artifact was modified as part of the governance reconciliation, STOP and report it rather than silently correcting it.

---

# 10. REPOSITORY CONSISTENCY CHECK

Perform a read-only repository consistency audit covering:

1. CR-001
2. Architecture Impact Assessment
3. Batch 1 implementation report
4. Batch 1 summary
5. security design boundary
6. relevant implementation files
7. frozen CP-01–06 specifications/checkpoints
8. Approved SRS

Verify that:

* terminology is consistent
* RBTP definition is consistent
* Batch 1 scope is consistent
* frozen boundaries are respected
* advisory findings are consistent
* no implementation claim contradicts evidence
* no governance document claims Di approval
* no fabricated live-Claude verification exists

---

# 11. TESTING

Do NOT add new implementation tests unless a purely documentation-validation test is genuinely necessary.

Run the existing regression suite once to confirm that governance reconciliation did not disturb the implementation.

Expected baseline from Batch 1:

**249 passed, 0 failed**

If the result differs:

**STOP → REPORT → DO NOT CLAIM SUCCESS.**

---

# 12. CREATE ONE GOVERNANCE RECONCILIATION REPORT

Create:

`docs/claude-execution-reports/CP-MVP2-CR-001/`

with a clearly named report such as:

`CP-MVP2-CR-001-GOVERNANCE-RECONCILIATION-<timestamp>-<id>.md`

The report must contain:

### A. Scope

Governance reconciliation only.

### B. Previous inconsistency

Explain the stale PROPOSED/PENDING state in the canonical CR documents.

### C. Reconciled state

State exactly what was reconciled.

### D. RBTP definition

Explicitly state:

**Risk-Based Test Prioritization**

### E. Batch 1 architectural decisions

List the approved decisions.

### F. Frozen boundaries

List CP-01–06, Approved SRS, and `llm/client.py` as protected.

### G. Advisories

Document:

1. `AUTOMATION_ID_COLLISION_ADVISORY`
2. live Claude RBTP verification PATH limitation

### H. Security boundary

State that security/penetration remains POST-MVP/PARKED.

### I. Regression

Record actual regression result.

### J. Repository consistency result

State whether the repository is internally consistent after reconciliation.

### K. Governance status

The report may state the reconciled CR governance state.

However:

**DO NOT declare Di's independent gate PASS.**

---

# 13. COMMIT

After all reconciliation and verification is complete:

1. Review `git diff`
2. Verify only intended governance/documentation files changed
3. Verify no frozen implementation/specification was modified
4. Run regression
5. Create the governance reconciliation report
6. Commit everything in ONE commit
7. Push to `origin/main`
8. Confirm:

`HEAD == origin/main`

---

# 14. FINAL RESPONSE FORMAT

Your final response must be concise and factual.

Include:

### Governance reconciliation

* what was reconciled

### Files changed

* exact files

### Frozen files verified unchanged

* CP-01–06
* Approved SRS
* `llm/client.py`

### Regression

* actual result

### Advisories

* automation ID collision
* live Claude RBTP PATH limitation

### Commit

* full commit hash
* HEAD/origin status

### Report

* GitHub path/link

Do NOT provide an approval/gate decision.

The final line must be:

**"Governance reconciliation complete. Awaiting Di's independent final gate."**

---

# 15. STOP CONDITIONS

STOP immediately and report if:

* any frozen specification requires modification
* any frozen implementation was modified
* the Approved SRS was modified
* CR-001 cannot be reconciled without changing substantive Batch 1 behavior
* evidence contradicts the proposed governance state
* regression fails
* Git state cannot be verified
* a claim would require fabricated evidence

Do not work around a stop condition.

---

## GOVERNANCE PRINCIPLE

**NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

**NEVER CHANGE THE IMPLEMENTATION TO HIDE A GOVERNANCE FINDING.**

**PERSISTENCE IS NOW THE ARCHITECTURAL DIRECTION; THIS TASK ONLY RECONCILES ITS GOVERNANCE RECORD.**

Implement → Verify → Report → Commit.

Then stop and wait for Di's independent gate.

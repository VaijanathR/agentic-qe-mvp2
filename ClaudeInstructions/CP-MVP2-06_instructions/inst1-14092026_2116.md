# CP-MVP2-06 — COMPLETE IMPLEMENTATION → VERIFICATION → FREEZE

## Single Batched Claude Execution Instruction

Repository:

`agentic-qe-mvp2`

We have completed and frozen:

* CP-MVP2-01
* CP-MVP2-02
* CP-MVP2-03
* CP-MVP2-04
* CP-MVP2-05
* CP-MVP2-06 specification

The CP-MVP2-06 specification is now a FROZEN CONTRACT.

Canonical specification:

`docs/CP-MVP2-06-SPECIFICATION-v1.0.md`

Specification SHA-256:

`acff2a219251e3343c94e971ce46787b2318605f27f88654c3626e459b14571d`

Do not modify it.

---

# 1. GOVERNANCE

Follow:

**SPECIFY → REVIEW → FREEZE → IMPLEMENT → VERIFY → GATE**

The specification phase is already complete.

This execution is:

**IMPLEMENT → VERIFY → FREEZE**

Do not redesign CP-06.

Do not modify the frozen CP-06 specification.

Do not modify any frozen CP-01 through CP-05 artifact.

Do not modify the Approved SRS.

Do not modify existing requirements, testcases, test data, or Playwright artifacts.

Do not begin CP-07.

Do not begin CP-08.

Do not begin CP-09.

### Golden rule

**NEVER CHANGE THE SPECIFICATION TO MAKE THE IMPLEMENTATION PASS.**

If implementation conflicts with the frozen specification:

**STOP → REPORT → DO NOT MODIFY THE SPECIFICATION → DO NOT SILENTLY WORK AROUND THE CONTRACT.**

Any genuine specification change requires a formal Change Request and Human + Di decision.

---

# 2. OBJECTIVE

Implement the frozen CP-MVP2-06 specification for:

## REAL BROWSER EXECUTION

The execution pipeline is:

**Accepted/Governed CP-05 Playwright Artifact**
→ **Deterministic Eligibility Validation**
→ **Real Chromium Headless Browser**
→ **Governed Step Execution**
→ **Deterministic Assertion Evaluation**
→ **Evidence Capture**
→ **Execution Result**
→ **Failure Classification**
→ **Governance Result**
→ **CP-06 Report**

CP-06 is an execution layer.

It must not become another testcase generator, test-data generator, or Playwright artifact generator.

---

# 3. FIRST ACTION — ESTABLISH BASELINE

Before changing anything:

1. inspect current branch
2. inspect current HEAD
3. inspect working tree
4. confirm origin/main
5. confirm CP-06 specification SHA-256
6. confirm CP-01 through CP-05 frozen state
7. run the existing regression suite
8. record the baseline result

Do not proceed blindly if the baseline is already failing.

If baseline failures exist, distinguish pre-existing failures from implementation failures.

---

# 4. READ THE FROZEN SPECIFICATION COMPLETELY

Read:

`docs/CP-MVP2-06-SPECIFICATION-v1.0.md`

Also inspect the actual CP-05 implementation and frozen artifacts.

Treat the specification and frozen upstream artifacts as authoritative.

Do not rely on memory or this instruction where the repository contains the actual contract.

---

# 5. IMPLEMENT ONLY CP-06

Create the CP-06 implementation required by the frozen specification.

Use clean separation from CP-03/04/05.

Do not modify frozen shared components unless the frozen CP-06 specification explicitly requires it.

Prefer new CP-06-specific modules.

Potential implementation areas include, as justified by the specification:

* execution schema
* eligibility validation
* browser executor
* step execution
* assertion execution
* evidence management
* failure classification
* execution result generation
* execution pipeline
* CP-06-specific tests

Use exact filenames/structure based on the repository's existing architecture and the frozen CP-06 specification.

Do not create unnecessary abstractions.

Do not rebuild existing functionality unnecessarily.

---

# 6. CP-05 IS THE EXECUTION CONTRACT

The implementation must consume CP-05 artifacts as governed inputs.

Do NOT:

* regenerate Playwright artifacts
* regenerate locators
* reinterpret requirements
* modify testcase intent
* modify test data
* change expected results
* silently repair unsupported artifacts
* convert unsupported artifacts into executable artifacts

If a CP-05 artifact is unsupported, CP-06 must respect that status.

---

# 7. EXECUTION ELIGIBILITY

Implement deterministic eligibility validation.

At minimum enforce the CP-05 governance statuses defined by the frozen specification.

The implementation must never execute an artifact that is:

* REJECTED
* QUARANTINED
* UNSUPPORTED_NEEDS_CLARIFICATION

unless the frozen specification explicitly defines otherwise.

If an artifact is not executable, produce the governed non-execution result.

Do not manufacture browser execution evidence.

---

# 8. BROWSER

MVP2 browser scope is:

**Chromium Headless**

Do not introduce:

* Firefox
* WebKit
* browser grids
* multi-VM execution
* cloud browser farms
* distributed execution
* parallel browser orchestration

unless explicitly required by the frozen CP-06 specification.

These are future capabilities.

---

# 9. REAL EXECUTION — NOT MOCKED

The implementation must support genuine Playwright/Chromium execution.

Tests may use mocks/stubs where appropriate for unit/integration testing.

However:

**mocked execution MUST NEVER be reported as live browser execution.**

For live verification, evidence must demonstrate:

* actual Playwright runtime
* actual Chromium launch
* actual SUT navigation/interactions where execution proceeds
* actual timestamps
* actual execution results
* actual evidence files

Do not fabricate screenshots, logs, or PASS results.

---

# 10. DATA-OQ-01 — CRITICAL LIVE-EXECUTION GOVERNANCE

This is a specific CP-06 risk.

The Demo Web Shop environment is a shared live SUT.

Before any state-mutating live execution, evaluate whether the test data can safely be used.

### Do NOT assume that synthetic CP-04 data is automatically safe for repeated live execution.

The implementation must not silently create uncontrolled persistent data pollution.

Before live state mutation, determine whether the required uniqueness/precondition strategy is sufficiently safe and evidenced.

If safe execution cannot be established from:

* existing approved test data
* repository configuration
* deterministic uniqueness controls
* known SUT behavior
* explicitly available environment controls

then:

## DO NOT PERFORM THE STATE-MUTATING LIVE TEST.

Instead produce a governed:

**BLOCKED** or **NOT_EXECUTED**

result according to the frozen specification.

Record:

* why execution was blocked
* which precondition was unavailable
* DATA-OQ-01 classification
* evidence supporting the decision
* what would be required for safe execution

A blocked result is a valid governance outcome.

Never weaken the data constraint merely to obtain a PASS.

Never rename or mutate test data simply to avoid a duplicate.

Never modify CP-04.

Never modify the SRS.

---

# 11. AUTHENTICATION / SESSION

There is currently no guaranteed reusable authenticated-session fixture.

Do not invent credentials.

Do not hard-code credentials.

Do not bypass authentication.

If authentication is required and no valid approved/safe authentication mechanism exists:

produce a governed BLOCKED/NOT_EXECUTED result.

Do not falsely report FAIL if the test could not legitimately start.

Do not falsely report PASS.

---

# 12. PRECONDITIONS

Implement deterministic precondition handling.

For every precondition determine:

* whether it is satisfied
* whether it can be verified
* whether it is unavailable
* whether execution can safely continue

If a required precondition cannot be established:

do not guess.

Use the governed status defined in the specification.

---

# 13. STEP EXECUTION

Implement the supported CP-05 action types:

* NAVIGATE
* FILL
* CLICK
* SELECT
* WAIT
* ASSERT

Use the locator specifications provided by CP-05.

Do not invent new locators.

Do not silently substitute a different locator because the existing locator fails.

If the artifact is internally inconsistent or cannot be executed safely:

classify the result according to the frozen governance rules.

---

# 14. ASSERTIONS

Respect the CP-05 assertion classes:

* BUSINESS_REQUIRED
* TECHNICAL_USEFUL
* DIAGNOSTIC_OPTIONAL

Implement their semantics exactly as specified.

Business assertions are the primary functional outcome.

Technical/diagnostic assertions must not silently override a business result.

Record assertion-level evidence.

---

# 15. NO SELF-HEALING

CP-06 must NOT implement self-healing.

Do not:

* search for alternative locators
* rewrite selectors
* change steps
* change expected results
* modify CP-05 artifacts
* automatically regenerate failed automation

A locator failure is evidence.

It must be classified and reported.

Future self-healing belongs to a separately governed capability.

---

# 16. RETRY POLICY

Do not introduce arbitrary retries.

Follow the frozen CP-06 specification.

If the specification does not authorize retry for a particular condition, do not retry it.

Do not use retry to hide instability.

Do not convert intermittent failure into PASS without evidence.

---

# 17. EVIDENCE

Implement evidence capture as defined by the specification.

At minimum, where applicable:

* execution log
* step result
* assertion result
* failure screenshot
* browser diagnostics
* final result
* evidence references

Every evidence item must be traceable to its execution.

Use deterministic paths/naming.

Do not expose secrets in logs/screenshots.

---

# 18. SECURITY / SECRET HANDLING

Never commit:

* passwords
* API keys
* authentication tokens
* secrets
* real payment credentials

Do not print secrets into logs.

If a password/token appears in page fields or runtime data, ensure evidence does not unnecessarily expose it.

Use synthetic CP-04 test data.

---

# 19. FAILURE CLASSIFICATION

Implement the frozen taxonomy.

At minimum preserve:

* requirement mismatch / ambiguity
* test-data issue
* authorization issue
* tool issue
* environment issue
* third-party dependency issue
* constraint violation

Where execution-specific categories are used, such as:

* locator failure
* assertion failure
* timeout
* browser launch failure
* navigation failure

map them consistently to the governed taxonomy.

Do not hide important distinctions.

---

# 20. DESIGN-TIME VS RUNTIME

Clearly distinguish:

### DESIGN-TIME

Artifact cannot legitimately execute.

Examples:

* unsupported CP-05 artifact
* invalid schema
* missing mandatory locator
* invalid governance status

### RUNTIME

Eligible artifact was actually launched/executed and failed during runtime.

Examples:

* navigation failure
* timeout
* locator failure
* assertion failure
* environment interruption

Do not report a design-time unsupported artifact as a runtime browser failure.

---

# 21. TRACEABILITY

Every execution result must preserve:

`execution_id`
→ `automation_id`
→ `testcase_id`
→ `requirement_id(s)`
→ `test_data_set_id`
→ `Approved SRS`
→ `source attribution`

No orphan results.

No fabricated IDs.

---

# 22. REPRODUCIBILITY

Capture available runtime metadata, including where available:

* execution ID
* timestamp
* browser
* browser version
* Playwright version
* Python/runtime version
* SUT URL
* repository commit
* artifact reference
* test-data reference
* environment information
* execution configuration

Do not claim audit-grade reproducibility if the environment does not support it.

---

# 23. TESTING THE IMPLEMENTATION

Create comprehensive CP-06 tests.

At minimum:

### Unit tests

For:

* schema
* eligibility
* result handling
* failure classification
* evidence handling
* governance decisions

### Integration tests

For:

* execution pipeline
* artifact loading
* step/result flow
* evidence generation
* governance propagation

### Negative tests

Cover:

* unsupported artifact
* invalid artifact
* missing locator
* invalid browser intent
* missing test data
* missing precondition
* unauthorized execution
* environment failure
* assertion failure
* timeout

### Security/data-handling tests

Verify secrets are not emitted into evidence/logging.

### Live browser test

Attempt at least one genuine browser verification IF AND ONLY IF the required safety/preconditions are established.

---

# 24. LIVE BROWSER VERIFICATION — IMPORTANT

Do not automatically assume a live PASS is required.

The first live verification must use an existing governed CP-05 artifact.

Do not create a new business scenario just for this test.

Before attempting it, determine:

1. Is there an ACCEPTED or POSSIBLE_DUPLICATE CP-05 artifact that is actually executable?
2. Is its locator evidence sufficient?
3. Are its required preconditions satisfiable?
4. Is authentication available if required?
5. Can its test data be used safely?
6. Does DATA-OQ-01 permit execution?
7. Can evidence be captured safely?

### If YES:

perform the genuine live browser execution and capture evidence.

### If NO:

DO NOT alter upstream artifacts.

DO NOT manufacture an executable artifact.

DO NOT fabricate live evidence.

Produce a governed BLOCKED/NOT_EXECUTED result explaining precisely why.

This is an acceptable and potentially valuable CP-06 governance result.

---

# 25. IMPORTANT — CURRENT KNOWN CP-05 CONDITIONS

Carry forward the actual known conditions from CP-05.

Do not assume they have been resolved.

Known areas include:

* REQ-REG-01 has zero locator evidence
* REQ-CART-03 has locator uncertainty
* REQ-PAY-01 has locator uncertainty
* payment option locator evidence is incomplete
* cart quantity selector remains unresolved
* DOM evidence is outside RAG by deliberate CP-02/CP-05 design
* ACTION_TYPE_EVIDENCE_ADVISORY remains open
* DATA-OQ-01 remains open/elevated
* no guaranteed reusable authenticated-session fixture

The implementation must handle these according to the frozen governance model.

---

# 26. DO NOT GAME COVERAGE

Do not modify eligibility rules simply to increase coverage.

Do not count BLOCKED/NOT_EXECUTED as PASS.

Do not report unsupported artifacts as executed.

Do not report mock execution as live execution.

Do not change requirements to improve coverage.

Do not alter test data to make the browser pass.

Coverage must reflect reality.

---

# 27. EXECUTION REPORT

After implementation and verification, create the detailed execution report under:

`docs/claude-execution-reports/CP-MVP2-06/`

Use the established naming convention.

The report must include:

* objective
* baseline
* implementation summary
* files created/modified
* tests
* regression
* live browser verification
* exact live execution status
* evidence locations
* failure classification
* DATA-OQ-01 assessment
* governance findings
* limitations
* upstream integrity verification
* specification SHA verification
* final status

Clearly distinguish:

* unit tests
* mocked/integration tests
* real browser evidence

---

# 28. SUMMARY

Create the concise summary under:

`docs/claude-execution-reports/CP-MVP2-06/summaries/`

Use the established naming convention.

---

# 29. FINAL IMPLEMENTATION-FREEZE CHECKPOINT

If implementation verification passes and there is no unresolved implementation blocker, create:

`docs/CP-MVP2-06-FINAL-IMPLEMENTATION-FREEZE-CHECKPOINT.md`

The checkpoint must record:

* implementation status
* implementation commit
* CP-06 specification SHA-256
* regression result
* CP-06 test result
* live browser result
* evidence references
* DATA-OQ-01 result
* upstream frozen-artifact integrity
* known advisories/limitations
* CP-07/08/09 status
* final governance status

A BLOCKED/NOT_EXECUTED live test does NOT automatically prevent implementation freeze if:

* the implementation behaves correctly
* the blocked result is deterministic and governed
* the reason is an environment/data/precondition limitation
* no implementation defect is responsible

---

# 30. UPSTREAM INTEGRITY

Before final commit, verify that the following are unchanged:

* CP-MVP2-01 artifacts
* CP-MVP2-02 artifacts
* CP-MVP2-03 specification
* CP-MVP2-03 implementation
* CP-MVP2-03 `llm/client.py`
* CP-MVP2-04 specification
* CP-MVP2-04 implementation
* CP-MVP2-04 freeze checkpoint
* CP-MVP2-05 specification
* CP-MVP2-05 implementation
* CP-MVP2-05 freeze checkpoint
* Approved SRS
* CP-MVP2-06 frozen specification

Use direct Git verification.

Do not rely solely on timestamps or file names.

---

# 31. CP-06 SPECIFICATION HASH

Verify that:

`docs/CP-MVP2-06-SPECIFICATION-v1.0.md`

still has SHA-256:

`acff2a219251e3343c94e971ce46787b2318605f27f88654c3626e459b14571d`

If it differs:

**STOP.**

Do not silently repair it.

Report the discrepancy.

---

# 32. NO DOWNSTREAM WORK

Do not create:

* CP-MVP2-07 specification
* CP-MVP2-07 implementation
* CP-MVP2-08 specification
* CP-MVP2-08 implementation
* CP-MVP2-09 specification
* CP-MVP2-09 implementation

CP-07, CP-08 and CP-09 remain NOT STARTED.

---

# 33. FULL REGRESSION

Run the complete repository regression after implementation.

Record:

* total tests
* passed
* failed
* skipped if applicable
* errors if applicable

The expected baseline before implementation is:

**166 passed, 0 failed**

The final count will legitimately increase because CP-06 tests are being added.

Do not expect the final total to remain 166.

Any regression failure must be investigated.

Do not modify frozen upstream code simply to restore a green build.

---

# 34. GIT DISCIPLINE

Use normal Git workflow.

Do not rewrite history.

Do not force-push.

Do not modify previous commits.

Create a new normal commit containing the CP-06 implementation and its governed evidence/checkpoint artifacts.

After push verify:

`HEAD == origin/main`

Working tree must be clean.

---

# 35. FINAL FREEZE DECISION

Freeze CP-06 implementation only if:

* implementation matches frozen specification
* tests pass
* regression passes
* no upstream frozen artifact changed
* specification hash is unchanged
* live verification outcome is honestly reported
* DATA-OQ-01 is governed
* no fabricated evidence exists
* no secrets were committed
* no downstream checkpoint was started
* working tree is clean
* commit is pushed successfully

If an implementation blocker remains:

DO NOT freeze.

Report:

**CP-MVP2-06 IMPLEMENTATION GATE: BLOCKED**

with exact evidence.

If the implementation is correct and all governance conditions are satisfied:

state exactly:

**CP-MVP2-06 FINAL GATE: PASS — IMPLEMENTATION FROZEN / CHECKPOINT CLOSED**

and:

**Implementation status: COMPLETE — FROZEN / CLOSED**

---

# 36. FINAL RESPONSE FORMAT

Your final response must provide:

1. Final status
2. Final commit SHA
3. CP-06 implementation documentation URL
4. CP-06 implementation/freeze checkpoint URL
5. Detailed execution report URL
6. Summary URL
7. Live browser execution result
8. Evidence URL(s)/paths
9. DATA-OQ-01 outcome
10. CP-06 test count/result
11. Full regression count/result
12. Specification SHA-256
13. Upstream frozen-artifact verification
14. Advisories/limitations
15. Explicit confirmation that CP-07/08/09 remain NOT STARTED

All GitHub URLs must be clickable.

Do not paste large code blocks into the final response.

---

# 37. FINAL PRINCIPLE

The objective is NOT:

**"Make the Demo Web Shop test pass."**

The objective is:

**"Demonstrate trustworthy, governed, evidence-backed real browser execution of an accepted Agentic QE automation artifact."**

Therefore:

**PASS when execution genuinely passes.**

**FAIL when genuine execution genuinely fails.**

**BLOCKED/NOT_EXECUTED when safe execution cannot legitimately occur.**

Never manufacture success.

Execute this complete CP-MVP2-06 implementation → verification → freeze cycle now.

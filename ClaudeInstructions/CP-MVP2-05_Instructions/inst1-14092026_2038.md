# CP-MVP2-05 — IMPLEMENTATION

Human + Di have formally approved and frozen the CP-MVP2-05 specification.

Frozen specification:

`docs/CP-MVP2-05-SPECIFICATION-v1.0.md`

Freeze commit:

`da10e89fd8fe0539a2c47569b74401b6abf4ebd7`

Specification SHA-256:

`7ab5638547e32d652addddfd91fc327a18bdb50fa9ecfe1048cac5875883c7ac`

## GOVERNANCE STATUS

**CP-MVP2-05 SPECIFICATION: FROZEN**

You are now explicitly authorized to implement CP-MVP2-05.

Follow:

**SPECIFY → REVIEW → FREEZE → IMPLEMENT → VERIFY → GATE**

We are now in:

**IMPLEMENT**

Do NOT modify the frozen specification.

Do NOT modify CP-MVP2-01, CP-MVP2-02, CP-MVP2-03, CP-MVP2-04, or the Approved SRS.

---

# 1. IMPLEMENT EXACTLY AGAINST THE FROZEN SPEC

Before changing code:

1. Verify current HEAD.
2. Verify clean working tree.
3. Verify frozen specification SHA-256.
4. Verify CP-01 through CP-04 remain unchanged.
5. Read the complete CP-MVP2-05 specification.
6. Read the relevant CP-03 and CP-04 contracts and implementation.
7. Establish the CP-05 implementation boundary.

If any frozen artifact differs:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not repair or overwrite it.

---

# 2. CP-MVP2-05 OBJECTIVE

Implement the governed transformation:

APPROVED SRS
→ GOVERNED EVIDENCE
→ ACCEPTED CP-03 TESTCASE
→ GOVERNED CP-04 TEST DATA
→ PLAYWRIGHT ARTIFACT
→ DETERMINISTIC VALIDATION
→ TRACEABILITY
→ GOVERNANCE

The implementation must generate structured, governed Playwright automation artifacts.

Do not treat raw generated source code as the only artifact.

---

# 3. REUSE EXISTING FOUNDATION

Reuse the existing MVP2 / CP-03 / CP-04 architecture where appropriate.

Do NOT duplicate frozen functionality unnecessarily.

Do NOT modify `llm/client.py` unless the frozen CP-05 specification explicitly requires a change.

If a frozen component appears insufficient:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not silently expand scope.

---

# 4. REQUIRED IMPLEMENTATION AREAS

Implement only what is required by the frozen CP-05 specification.

This should include, as applicable:

* CP-05 Playwright artifact schema
* Playwright step representation
* locator representation/governance
* testcase → automation mapping
* test-data → UI field mapping
* expected-result → assertion mapping
* source attribution
* deterministic validation
* governance status
* traceability
* coverage
* unsupported/needs-clarification handling
* duplicate detection where specified
* LLM generation integration where specified
* CP-05 pipeline/orchestration
* CP-05 tests
* CP-05 implementation documentation

Do not add unrelated architecture.

---

# 5. CRITICAL LOCATOR RULE

This is mandatory:

**NEVER FABRICATE A LOCATOR.**

Locator evidence must come from an approved/governed evidence source defined by the frozen specification.

If locator evidence does not exist:

`UNSUPPORTED / NEEDS CLARIFICATION`

or the exact governed equivalent defined in the specification.

Do not guess based on:

* memory of the website
* generic Playwright knowledge
* likely HTML
* assumed CSS classes
* assumed IDs
* arbitrary XPath
* generated DOM hierarchy

In particular:

### REQ-REG-01

Current evidence has zero confirmed registration locator evidence.

Do not invent one.

### Cart quantity selector

The evidenced selector:

`itemquantity7079994`

is item-specific.

Do not assume it is a reusable generic selector unless evidence supports that conclusion.

### Payment method

Do not invent per-option locators where exact evidence is unavailable.

---

# 6. TEST DATA INTEGRITY

Every generated UI input must map to CP-04 governed test data.

Example:

CP-04 field:

`email`

must map deterministically to the corresponding Playwright input.

Do not replace governed values with invented values.

Do not generate fresh test data inside CP-05 to compensate for missing CP-04 data.

If required data is missing:

`UNSUPPORTED / NEEDS CLARIFICATION`

---

# 7. ASSERTION INTEGRITY

Assertions must derive from governed testcase expected results and approved evidence.

Separate:

* required business assertions
* technical assertions
* optional diagnostics

Optional diagnostics must never inflate requirement coverage.

---

# 8. TRACEABILITY

Every accepted Playwright artifact must be traceable:

Playwright artifact
→ CP-03 testcase
→ requirement ID
→ Approved SRS / governed evidence

And every input:

Playwright input
→ CP-04 dataset
→ CP-04 field

Reject or quarantine orphan artifacts.

Unknown IDs must never be accepted.

---

# 9. GOVERNANCE

The LLM proposes.

Deterministic code validates and decides.

The LLM must never self-authorize:

* ACCEPTED
* REJECTED
* QUARANTINED
* UNSUPPORTED
* DUPLICATE
* POSSIBLE_DUPLICATE

Use the governance vocabulary defined in the frozen specification.

---

# 10. COVERAGE

Implement deterministic CP-05 coverage.

Coverage denominator:

**applicable accepted CP-03 testcases**

Coverage numerator:

**valid governed CP-05 automation artifacts**

Do not count:

* rejected artifacts
* quarantined artifacts
* orphan artifacts
* unsupported artifacts
* invalid artifacts

toward coverage.

---

# 11. CP-06 BOUNDARY

Do NOT execute generated Playwright tests against a real browser.

Do NOT start CP-MVP2-06.

CP-05 may generate automation artifacts and perform deterministic validation only.

Real browser execution belongs to CP-06.

---

# 12. NO SELF-HEALING

Do not implement self-healing.

Do not automatically modify:

* requirements
* testcases
* test data
* expected results
* governed locators

to make generation succeed.

Where appropriate, preserve evidence that future CP-07 RCA/replanning can consume.

---

# 13. SECURITY / DATA

No real PII.

No real payment credentials.

No secrets.

No hard-coded production credentials.

Use configuration/environment variables where needed.

Do not introduce production infrastructure.

---

# 14. TESTING

Create focused CP-05 tests covering at least:

* valid artifact generation
* schema validation
* testcase traceability
* requirement traceability
* test-data traceability
* locator evidence
* unsupported locator handling
* fabricated locator rejection
* cart-selector non-generalization
* missing test-data handling
* assertion governance
* governance states
* coverage
* duplicate/orphan handling where specified
* regression protection

Also test the actual integration path sufficiently to demonstrate that the CP-05 pipeline works.

---

# 15. LIVE LLM VERIFICATION

If the existing Claude Pro provider is used:

Perform controlled live verification through the actual CP-05 pipeline.

Do not confuse a unit-test stub with real end-to-end LLM verification.

Record:

* invocation count
* provider
* latency
* raw/structured response handling
* schema validation
* governance result
* traceability result
* coverage result
* failures/retries
* final output

Do not make unnecessary repeated live calls.

---

# 16. APPLICATION EVIDENCE

Respect the CP-05 specification's evidence boundary.

Where raw discovery/DOM evidence is required, read it from the governed raw evidence source defined by the specification.

Do NOT assume that DOM/locator evidence is available through the CP-02 KnowledgeBase if the specification says it is not.

Do not invent missing evidence.

---

# 17. REGRESSION

Before implementation, establish the existing baseline:

**122 passed, 0 failed**

After implementation:

Run the complete regression.

The existing 122 tests must continue to pass.

New CP-05 tests must be reported separately.

If regression breaks:

**STOP → REPORT → HUMAN + DI DECIDE**

Do not modify frozen artifacts to restore the baseline.

---

# 18. IMPLEMENTATION DOCUMENTATION

Create:

`docs/CP-MVP2-05-IMPLEMENTATION.md`

Document:

* architecture
* modules
* data flow
* schemas
* validation
* governance
* locator handling
* traceability
* coverage
* known limitations
* test strategy
* live verification
* downstream CP-06 contract

---

# 19. EXECUTION REPORT

Create a detailed report under:

`docs/claude-execution-reports/CP-MVP2-05/`

Include:

* starting commit
* frozen specification SHA-256
* files created
* files modified
* files explicitly not modified
* architecture decisions
* tests
* regression
* live verification
* governance outcomes
* coverage
* known limitations
* final repository state
* commit SHA

---

# 20. EXECUTION SUMMARY

Create:

`docs/claude-execution-reports/CP-MVP2-05/summaries/`

The summary must include:

* objective
* implementation status
* test result
* regression result
* live verification result
* governance result
* coverage
* limitations
* commit

---

# 21. GIT

Use normal commits.

Do NOT amend previous commits.

Keep CP-05 implementation commits logically separated from documentation/evidence where practical.

Push to:

`origin/main`

Verify:

`HEAD == origin/main`

and clean working tree.

---

# 22. FINAL RESPONSE

Return mandatory clickable GitHub URLs for:

1. CP-MVP2-05 implementation documentation
2. detailed execution report
3. execution summary
4. implementation commit(s)
5. final HEAD commit

Also report:

* baseline regression
* final full regression
* CP-05 focused test result
* live Claude invocation result
* governance result
* coverage
* limitations
* frozen artifacts verification
* whether CP-06 was started

Do NOT claim CP-05 is frozen merely because implementation passes.

After implementation, status is:

**IMPLEMENTED — PENDING HUMAN + DI VERIFICATION**

A separate verification/final-freeze gate will follow.

---

# 23. ABSOLUTE RULE

**NEVER CHANGE THE FROZEN SPECIFICATION TO MAKE IMPLEMENTATION PASS.**

If implementation conflicts with the frozen specification:

**STOP → REPORT → HUMAN + DI DECIDE**

If a requirement/evidence/locator is missing:

**DO NOT INVENT**
